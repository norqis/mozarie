# 実機確認

対象: commit __________ / VERSION __________
環境: Windows __________ / GPU __________ / Mozarie実行環境 __________
判定: Pass / Fail / NA と結果・証跡を記録

自動テストは禁止。以下を実機で確認する。

## 1. setup / run / update / 接続回復

| ID | 操作 | 期待値 | 結果/証跡 |
| --- | --- | --- | --- |
| SR-01 | 新規展開先で `setup.bat` を実行 | 依存関係とモデル準備が完了し、誤った失敗表示が出ず、管理者権限を要求しない | |
| SR-02 | `run.bat` で起動し、ブラウザを開く | 初期画面、設定、画像読込が利用できる | |
| SR-03 | 起動中に接続を切り、復帰させる | 「Mozarieに接続できません」だけを設定ボタン左に赤字表示し、復帰後に消える | |
| SR-04 | `update.bat` で更新確認と適用 | 更新案内、再起動、VERSION更新、既存設定と作業データの保持が正しい | |
| SR-05 | setup・起動・更新で失敗を起こす | 実原因に対応するエラー名だけを表示し、誤案内・不要ログを出さない | |
| SR-06 | `http.py` がある配置でsetup・起動する | `HTTPStatus` の循環importやモジュールshadowが再発しない | |

## 2. 読込 / gallery / scroll / filter

| ID | 操作 | 期待値 | 結果/証跡 |
| --- | --- | --- | --- |
| GL-01 | 画像・フォルダー選択とドラッグ&ドロップで読込 | 対象だけが一覧へ入り、サムネイルが先頭から正しく表示される | |
| GL-02 | 同じフォルダーを別PJで読込 | 既存PJの警告を表示し、既存PJを開く/別PJとして作る選択が意図どおり動く | |
| GL-03 | 読込後に元画像を変更・削除してPJを開く | 対象画像だけ変更警告を出し、選択時のみマスク・除外削除を反映する | |
| GL-04 | 左一覧を縦横スクロールし、未選択画像を右クリック | スクロール位置と選択画像を勝手に中央移動しない | |
| GL-05 | フィルター、フォルダー、検索、非表示、確認済みを切替 | 件数・対象・表示が一致し、確認済みは緑、未確認は正しい色と幅で表示する | |
| GL-06 | 400件を読込して一覧・対象切替 | 表示と一括対象が全件一致し、操作中に固まらない | |
| GL-07 | 壊れた画像、回転画像、PNG/JPEG/WebPを読込 | 読込可否と失敗理由が正しく、正常画像の一覧・向き・サムネイルを壊さない | |
| GL-08 | 2件以上選択して一括削除の確認を開き、取消・確定する | 確認の件数が一致し、取消は不変。確定時は選択分だけDB上の履歴・モザイク・除外・状態を削除し、元画像を維持する。単一削除の文言は既存のまま | |

## 3. PJ / 永続 / 完了 / 再開 / Undo

| ID | 操作 | 期待値 | 結果/証跡 |
| --- | --- | --- | --- |
| PJ-01 | projectlessで作業後に名前保存、PJ作成・名前変更、管理表のPJ名・作成日時・更新日時ヘッダーで昇降順ソート | 重複名を拒否し、PJ情報とソート結果が正しい | |
| PJ-02 | 画像追加、候補変更、手書き、確認済み、非表示後に再起動してPJを開く | 各画像の状態、候補、モザイク、除外、操作履歴が復元される | |
| PJ-03 | 画像一覧から画像を削除 | 一覧・PJデータ・候補・モザイク・除外・履歴から対象だけを削除する | |
| PJ-04 | PJ完了を実行 | 警告後に一覧を閉じ、PJ一覧は完了、各画像状態は保持する | |
| PJ-05 | 完了PJを開く | 閲覧・移動・ダウンロードだけ可能で、編集・検出・保存・Undo/Redoはできない | |
| PJ-06 | 完了PJで「作業を再開」を実行 | 作業中へ戻り、既存候補の個別ON/OFF・削除・強制ON/OFF・枠px・一括操作が直ちに再有効化する | |
| PJ-07 | 各画像で手書き・候補・一括操作後に Ctrl+Z / Ctrl+Shift+Z とUndo/Redoボタンを使う | PJ内の該当画像の履歴だけを順に戻し・進め、モザイク適用も対象になる | |
| PJ-08 | 管理画面で別PJと現在PJを削除 | 警告に選択PJ名、履歴・モザイク・除外データ削除、元画像不変、取消不能を表示し、別PJ削除は現在作業を変更しない | |
| PJ-09 | 現在の作業ダイアログを開く | 新規作成・管理と現在の作業の2カードになり、ZIPと削除は表示しない。PJなし画像0件だけ「作業を閉じる」が無効である | |
| PJ-10 | プロジェクト管理を開き、各行のPJ名以外のセルを単クリックする | PJ名、状態、画像数、元フォルダー、作成日時、更新日時、操作の表だけを表示し、下部の共有操作・選択詳細・プルダウンはない。操作ボタン以外の単クリックは何もしない | |
| PJ-11 | PJ名、作成日時、更新日時の表ヘッダーをクリックして昇降順に切替える | 該当列だけが並び替わり、activeの▲/▼、inactiveの細い▲▼、aria-sort、headerのfocusが現在の順序と一致する | |
| PJ-12 | 別PJの行でモザイク・除外ZIPを保存 | その行のPJだけをZIP化し、現在PJの未確定編集・開閉・表示を変更しない | |
| PJ-13 | 現在PJの行で候補を直前に編集してモザイク・除外ZIPを保存 | 候補更新と作業データを反映したZIPになる | |
| PJ-14 | 完了PJの行でモザイク・除外ZIPを保存 | 閲覧専用のまま両ZIPを保存できる | |
| PJ-15 | 画像0件のPJを確認する | その行の2種類のZIPだけが無効で、開く・削除は利用できる | |
| PJ-16 | 表のPJ名、モザイクZIP、除外ZIP、削除をTabとEnterで操作し、Escape・閉じる後のfocusを確認する | PJ名だけが開き、各行の操作だけが対象PJへ作用し、Tab順と閉じた後のfocus復帰が正しい | |
| PJ-17 | 日本語・英語、長いPJ名・元フォルダーで通常幅と720px以下の管理画面を開く | 両言語の表見出し・操作文言が揃い、通常幅はPJ名・元フォルダーを1行省略し全体をtitle表示する。720px以下は見出し付きカード表示で折返し、横はみ出しがない | |
| PJ-18 | 任意の行でZIP保存を失敗させる | 管理表と各行を保持し、その行のZIP操作だけが再操作可能に戻る | |
| PJ-19 | 管理画面の初回取得・ヘッダーソート・削除を失敗させる | 初回は現在の作業画面を維持し、ソート失敗時は適用済みの並び順とheaderの表示・focusを維持し、削除失敗時は対象行を残す。削除成功時は再取得せず対象行だけを消し、0件なら空状態を表示して閉じるへfocusする。 | |
| PJ-20 | 確認済み・未確認を混在させて単一・一括検出と途中キャンセルを行い、Undo/Redo後にPJ再開・鯖再起動する | 検出対象を含む全画像の確認状態が不変である | |
| PJ-21 | 新規作成・後保存・名前変更で完全一致・大小文字違い・前後空白のPJ名を入力する | 重複は専用エラーとなり、失敗時は現在のPJ・画像状態を維持する | |
| PJ-22 | 複数のネイティブフォルダーを持つPJで一部フォルダーを削除して開く | 読み込める画像は表示し、不足フォルダーの再指定を要求する | |
| PJ-23 | projectlessの候補・手書き・確認済み・非表示を名前保存中に失敗させる | PJの部分データを残さず、現在の作業をそのまま維持する | |
| PJ-24 | すでに指定値の一括非表示・一括確認済みを実行してCtrl+Zを使う | 状態不変でUndo/Redo履歴・履歴groupを追加しない | |

## 4. 検出 / model ON-OFF / 指定3画像

| ID | 操作 | 期待値 | 結果/証跡 |
| --- | --- | --- | --- |
| DT-01 | 各モデルを1つずつON/OFFし、標準・最高精度で自動検出 | 対象モデルだけをロード・実行し、OFFの候補は生成されず、他モデルのON/OFFに影響しない | |
| DT-02 | ターゲット、NTD11、Sensitive、Hand、HandSeg、SAM、白い液の全組合せをON/OFF | 設定保存後の再起動を含め、候補・除外・実行状況が設定どおりになる | |
| DT-03 | 白い液をOFFにして自動検出 | 白い液候補・白い液除外・白い液由来の追加候補を生成しない | |
| DT-04 | `Scene_cowgirl_00023.png` を最高精度で検出 | 陰茎を手として除外せず、陰茎・膣・白い液の候補を正しく扱う | |
| DT-05 | `Scene_00060.png` を最高精度で検出 | 目立つ白い液、手、膣を検出し、除外とモザイク範囲が正しい | |
| DT-06 | `Scene_00072.png` を最高精度で検出 | 目立つ白い液を検出し、除外範囲へ反映する | |
| DT-07 | 候補名、上段のON/OFF→検出範囲→適用範囲、各行のOFF/ON→検出範囲→適用範囲、強制ON/OFF、枠pxを確認 | 「手」「白い液」など本来の名称だけを表示し、順序と設定結果が一致する | |
| DT-08 | 枠pxへキーボード上下、CPU/CUDA/DirectMLと複数GPUを選択して検出 | 数値操作中にページがスクロールせず、provider・物理GPU identityどおりに実行する | |
| DT-09 | 壊れた・不足したモデルで検出を開始 | 別モデルやCPUへfallbackせず、原因どおりの文言を表示する | |
| DT-10 | 初回を含む80枚検出を開始 | 80/80で固着せず、準備中とファイル名が交互点滅しない | |

## 5. mosaic / exclusion / 2画面 / 全mouse-button

| ID | 操作 | 期待値 | 結果/証跡 |
| --- | --- | --- | --- |
| MX-01 | モザイク筆・消しゴム・塗りつぶし・境界をドラッグ | 描画中からリアルタイムで反映し、カーソル円が遅れず、除外判定は確定後に反映する | |
| MX-02 | 除外筆・除外消しゴム・除外塗りつぶしを使う | 除外だけを編集し、モザイク範囲へ手書きが混入しない | |
| MX-03 | 左クリック、右クリック、中クリック、ホイール、Shift+ホイール、ドラッグ、ダブルクリックを全ツールで操作 | それぞれの割当だけが動作し、意図しない選択・スクロール・十字カーソル・描画が起きない | |
| MX-04 | 1画面/2画面を切替え、画像・マスク双方をパン・ズーム | 両画面が同期し、左右どちらからも編集でき、変更は即時に両方へ反映する | |
| MX-05 | 2画面でモザイク・除外をクリアし、表示範囲を切替 | 右の色・マスク・候補表示に残像が残らない | |
| MX-06 | 3本のsplitterを単クリック・3px超ドラッグ・ダブルクリックし、左右ペインを確認する | 単クリックまたは1〜2px移動では幅が変わらない。3px超は追従して保存され、ダブルクリックは表示幅に応じた既定値へ戻る。左右ペインに横スクロール・横はみ出しがない | |
| MX-07 | 境界の矩形、4点ポリゴン、ブラシを作成・取消・検出 | 指定ROIだけに境界候補を作成し、取消と候補反映が正しい | |
| MX-08 | 1/2表示ボタンの整列、中央splitter、2画面右の黒背景を確認 | ボタンが整列し、中央幅を変更でき、右背景は画像サイズに一致する | |
| MX-09 | 2画面でpreview ON、ON+blink、OFF+blinkを切替 | ONは左モザイク・右固定色、ON+blinkは左同・右点滅、OFF+blinkは左元画像・右点滅になる | |

## 6. 単一 / 一括save / download

| ID | 操作 | 期待値 | 結果/証跡 |
| --- | --- | --- | --- |
| SV-01 | 単一保存を開き、末尾を入力後に全消去してfocus移動・保存先変更し、空のまま保存後に一括保存を開く | 単一の初期値・操作後は末尾が空で、空保存は同名なしなら元名、同名ありなら連番になる。一括は既定`_censored`で値を共有しない | |
| SV-02 | 単一保存でモザイク有無・除外有無・出力先をブラウザのdirectory pickerで切替 | 選択した現在画像だけを元画像と同じ形式・指定した名前で保存する | |
| SV-03 | 一括保存を400件で実行 | 一覧にある全画像を毎回対象にし、確認済み・既出力の有無で対象を勝手に外さない | |
| SV-04 | 単一・一括保存の前後で候補、確認済み、非表示、選択状態を比較 | 保存だけが行われ、画像一覧削除や候補削除など他状態を変更しない | |
| SV-05 | 画像ごと、PJ全体のモザイク・除外ダウンロードを実行 | 画像ごとは正しい範囲、PJ全体はZIPで全対象をダウンロードする | |
| SV-06 | コピー保存で「コピー成功後に元画像を削除」を選び、保存確定をunknown・確定的失敗/pending・復元失敗にする | unknownは新しい出力を保持する。確定的失敗/pendingは元画像を復元してから新しい出力を削除し、復元失敗時は出力を保持する。いずれも完了扱いにしない | |

## 7. settings / i18n / error / performance / RAM / VRAM / release

| ID | 操作 | 期待値 | 結果/証跡 |
| --- | --- | --- | --- |
| ST-01 | 設定のモデル、GPU、精度、色許容、塗りつぶし曖昧さ、表示、保存を変更して再起動 | 各設定が永続し、候補・検出・表示へ正しく反映する | |
| ST-02 | 色許容ダイアログを開閉・変更後に再起動 | ダイアログが閉じ、色許容値が永続して塗りつぶしへ反映する | |
| ST-03 | 日本語・英語とモデルダウンロードを切替 | モデルダウンロードを含め直書き文字列・未翻訳キーを出さない | |
| ST-04 | 検出・保存・接続・設定の各失敗を起こす | 原因と次にする操作が一致し、別原因の誤エラーを出さない | |
| ST-05 | idle、4K、一覧大量、5回検出、100件・400件一括を連続操作 | 応答時間が実用的で、RAM/VRAMが増え続けず完了後に不要分を解放する | |
| ST-06 | リリース前にChecks backend/frontend、PR、merge、tag、asset、main同期、merge済branch削除を確認 | required contextとリリース成果物が揃い、mainとローカルが同期し不要branchが残らない | |
| ST-07 | リリースZIPを新規フォルダーで展開し、setup・起動・主要操作・更新を確認 | 必要ファイルだけを含み、不要ログ・開発成果物・旧DB・互換処理を含まない | |
| ST-08 | 既存workspace DBのPJ名一意制約を確認して起動する | `name COLLATE NOCASE UNIQUE` が欠けるDBを再作成対象として止める | |
