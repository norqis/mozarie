# 起動・読み込み・一覧・プロジェクト

[共通確認](../manual-verification.md)を全行で実施する。

| ID | 手順 | 期待値 |
| --- | --- | --- |
| WS-143 | ブラウザーで親・子・孫フォルダーに同名画像を置いたPJへ親フォルダーを追加し、再読込後に孫の画像を削除または保存する。初回選択、許可が失われたフォルダーの「復元」、選択取消を分けて確認する | 初回選択とフォルダーの明示復元は削除に必要な書込み許可を求める。再読込後も保存済みrequest IDから同じbrowser-directory sourceのAPI正規IDへ復元し、孫画像だけを孫フォルダーから削除または保存して親・子の同名画像を残す。許可がない自動復元は確認を出さず、取消は一覧と読込状態を保持する。 |
| WS-142 | 無名作業でフォルダーを読み込み手描きした後、同じパスを読み込み直して再編集する。再起動して復元後、別フォルダーを読み込み手描きする。 | 同じフォルダーの再読込では画像ID・手描き・履歴が保持され、再編集と再起動後の復元が成功する。別フォルダーへの切替後も手描きが保存される。 |
| WS-141 | 非現在画像の右クリックまたはF2で名前変更を開く。編集名を入力し、取消・OK・「元画像の名前に戻す」を確認する。元フォルダー未接続でも編集し、PJを開き直す。 | 中央モーダルの左下に元名復元、右下にOKとその左に取消を表示する。復元は入力だけを戻し、OKで保存名を確定する。元ファイルと元の相対名、画像ID・候補・手描き・状態・反転・履歴は変更せず、編集名を別に保持して再表示する。 |
| WS-001 | 新規展開先でsetup.batを実行 | 依存準備と実行環境確認が完了し、完了案内が出る |
| WS-002 | setup.batを再実行 | 既存環境の確認が完了し、二重起動と誤認しない |
| WS-003 | run.batで起動 | ページを開け、設定と画像読み込みが使える |
| WS-004 | 画像0件の新規DBで起動 | 画像位置は未選択表示。保存・検出は無効、設定・読み込みは有効 |
| WS-005 | 接続を止める | 設定ボタン左に「Mozarieに接続できません」を赤字で表示する |
| WS-006 | 接続を復帰 | 赤い接続案内が消え、再操作できる |
| WS-007 | 使用中ポートで起動 | 使用中ポートを示し、別原因の依存エラーを表示しない |
| WS-008 | 画像読み込みメニューを開閉 | パス入力・画像選択・フォルダー選択が表示され、閉じると消える |
| WS-009 | フォルダーパスを入力して読み込む | 配下の対応画像が一覧へ入り、初期スクロール位置が先頭となる |
| WS-010 | フォルダーAを読み込み画像を表示した後、別フォルダーBのパスを入力して読み込む | Aの画像・編集中画像を残さずBの画像だけへ置き換わる。件数・サムネイル・編集対象・入力パスがBと一致し、初期スクロール位置が先頭となる |
| WS-011 | 名前付きPJで元フォルダーAを開いた状態から、通常のパス入力で別フォルダーBを読み込む | BをPJへ追加せず、画面はBだけの無名作業へ切り替わる。元PJを開き直すとAの画像・編集状態を復元する |
| WS-012 | 画像ファイルをドロップ | ドロップ画像を追加し、ブラウザーを画像URLへ遷移させない |
| WS-013 | フォルダーをドロップ | 配下の対応画像を追加し、処理後にドロップ案内が消える |
| WS-014 | 名前付きPJでフォルダーAを開き、400画像など読み込みに時間がかかる別フォルダーBを通常のパス入力で読み込む。Bの読み込み中にAの複数画像を選択して一括確認済み・一括非表示を実行し、別画像では反転または手描き編集も行う | Bの読み込み中の操作はAのPJへ保存される。Bの読み込みは破棄されず完了し、画面はBだけの無名作業へ切り替わる。元PJ Aを開き直すと、一括確認済み・一括非表示と反転または手描き編集をすべて復元する |
| WS-016 | 開けない画像と、別の画像ビューアーで開けるPNGを同時に読み込む | 開けないファイル名を案内し、PNGのサムネイルと編集画像を表示する |
| WS-017 | 透過PNGを読み込む | 透過部分と画像の形状を保持する |
| WS-018 | 回転情報付きJPEGを読み込む | サムネイルと編集画像の向きが一致する |
| WS-019 | 長い日本語名の画像を読み込む | 文字化けせず、名前が隣の操作へ重ならない |
| WS-020 | 同名画像を別フォルダーから追加 | 両画像を別の画像として扱い、片方の編集をもう片方へ反映しない |
| WS-021 | 読み込みを一時停止 | 進行中の処理が落ち着いた後に新しい画像の読み込みが増えない |
| WS-022 | 読み込みを再開 | 未処理画像から進捗が再開する |
| WS-023 | 読み込みをキャンセル | 完了済みの扱いを画面に示し、新規処理を止め、操作を再有効化する |
| WS-024 | 400画像を読み込む | 画像数と一覧対象が一致し、先頭・途中・末尾のサムネイルが表示される |
| WS-025 | 左一覧を下へスクロールし、未選択画像を右クリック | スクロール位置と編集中画像を変えず、その画像のメニューを開く |
| WS-026 | 画像メニューから元パスをコピー | 対象画像の元パスをコピーし、選択画像を変更しない |
| WS-027 | 画像メニューから確認済みにする | その画像だけ確認済みになり、表示は緑になる |
| WS-028 | 確認済み画像を未確認に戻す | その画像の確認済み表示が消え、未確認は緑にならない |
| WS-029 | 画像メニューから非表示にする | 対象だけ通常一覧から隠れ、元ファイルと編集データは残る |
| WS-030 | 非表示画像を再表示 | 通常一覧へ戻り、編集データを保持する |
| WS-031 | 前へを押す | 表示順で1つ前の画像へ移動する。先頭を越えない |
| WS-032 | 次へを押す | 表示順で1つ次の画像へ移動する。末尾を越えない |
| WS-033 | 左一覧でM・U・R・N・Hの各組合せを選び、先頭・中間・末尾・1件・絞り込み外の現在画像から「確認済みにして次へ」を実行する | 押下時の左一覧フィルター順だけをスナップショットとして使う。現在画像が確認済み化で一覧から外れても、次の一致画像を飛ばさず開く。絞り込み外なら先頭の一致画像を開く。末尾と1件で次がなければ、確認済みになった現在画像とキャンバスを保持し、直前の画像へ戻らない |
| WS-034 | 左一覧でM・U・R・N・Hの各組合せを選び、先頭・中間・末尾・1件・絞り込み外の可視な現在画像から「非表示にして次へ」を実行する | 押下時の左一覧フィルター順だけをスナップショットとして使う。次の一致画像を開き、絞り込み外なら先頭の一致画像を開く。末尾と1件で次がなければ、非表示になった現在画像とキャンバスを保持し、直前の画像へ戻らない。Hで表示する既に非表示の現在画像は閲覧できるが、処理対象にはしない |
| WS-035 | 削除を取消 | 現在画像と一覧・編集データを保持する |
| WS-036 | 元画像削除を確定 | 対象の元画像・一覧・PJデータを完全削除し、表示中フィルターの次画像へ移動する |
| WS-037 | 一覧表示を開く | 画像一覧とフィルター・検索・フォルダー選択を表示する |
| WS-038 | 一覧表示を閉じる | 編集中画像へ戻り、その編集を保持する |
| WS-039 | 左一覧と一覧表示で「絞り込み」を開き、フィルターをすべて外して閉じる。狭い幅、一覧切替、左ペインを閉じる操作も行う | 両画面でボタン直下に縦並びの項目が開き、画面端では表示域内へ収まる。Esc・外側クリックで閉じ、Tabで項目と続く操作へ標準どおりフォーカス移動できる。画面切替・ペイン折りたたみで浮いたメニューを残さない。閉じた後も選択状態と検索・フォルダーの絞り込みを保持し、非表示も含む全画像を表示する |
| WS-040 | モザイクありだけを選ぶ | 非表示ではなく、除外差引き後も有効なモザイク範囲がある画像を、確認状態に関係なく表示する |
| WS-041 | モザイクありを選んだ画像の候補を更新し、モザイクなしだけを選ぶ | 非表示ではなく、更新後に有効なモザイク範囲が0の画像だけを表示し、更新前のモザイク状態を残さない |
| WS-042 | 確認済みとモザイクありを同時に選ぶ | 非表示ではなく、確認済みまたはモザイクありの画像を表示する |
| WS-043 | 未確認とモザイクなしを同時に選ぶ | 非表示ではなく、未確認またはモザイクなしの画像を表示する |
| WS-044 | 非表示だけを選び、次にモザイクありも選ぶ | 前者は非表示画像だけを表示する。後者は非表示画像と通常表示のモザイクあり画像を表示する |
| WS-045 | 一覧の検索へファイル名の一部を入力 | 一致する名前だけ表示する。空欄へ戻すと検索による絞り込みが解除される |
| WS-046 | 一覧のフォルダーを選択 | そのフォルダーの画像だけを表示する |
| WS-047 | 一括編集を開始 | 画像選択操作と選択数を表示する |
| WS-048 | Ctrl+クリックで2画像を選択 | クリックした画像を個別に選択・解除する |
| WS-049 | Shift+クリックで範囲選択 | 起点から終点までの表示順の範囲を選択する |
| WS-050 | 一括編集を終了 | 一括選択が解除され、通常の画像操作へ戻る |
| WS-051 | 2画像を選択して一括確認済み | 選択画像だけを確認済みにする。未選択画像の状態を保持する |
| WS-052 | 2画像を選択して一括未確認 | 選択画像だけを未確認にする。未選択画像の状態を保持する |
| WS-053 | 2画像を選択して一括非表示 | 選択画像だけを非表示にする。未選択画像の状態を保持する |
| WS-054 | 2画像を選択して一括再表示 | 選択画像だけを再表示にする。未選択画像の状態を保持する |
| WS-055 | 2画像を選択して一括削除を取消 | 選択数・一覧・PJデータが変わらない |
| WS-056 | 2画像を選択して一括削除を確定 | 指定2画像のPJデータだけを消し、元ファイルと未選択画像を保持する |
| WS-057 | 画像切替中に保存・検出・候補編集を押す | 現在画像操作が無効になり、切替前の画像へ遅れて処理しない |
| WS-058 | PJを作らず画像を編集して名前保存 | 既存候補・手描き・確認済み・非表示を作成PJへ引き継ぐ |
| WS-059 | 新規PJを作成 | 入力名のPJを作り、元PJの編集を保持する |
| WS-060 | PJ名を変更 | 一覧と現在PJの名前が更新され、画像・履歴は残る |
| WS-061 | 同名PJを作成 | 重複名を案内し、既存PJを置換しない |
| WS-062 | 大小文字・前後空白だけ異なるPJ名を入力 | 同一名として拒否し、現在の作業を保持する |
| WS-063 | PJ名入力を取消 | PJの追加・名前変更を行わない |
| WS-064 | PJ管理を開く | 名前・状態・画像数・元フォルダー・作成日時・更新日時・行操作を表で表示する |
| WS-065 | PJ名をクリック | その行のPJを開く |
| WS-066 | PJ名以外の通常セルをクリック | PJを開かず、選択による別操作パネルを出さない |
| WS-067 | 名前ヘッダーを昇順へ切り替える | 名前の昇順に行を並べ、その列の昇順三角を表示する |
| WS-068 | 名前ヘッダーを降順へ切り替える | 名前の降順に行を並べ、その列の降順三角を表示する |
| WS-069 | 作成日時ヘッダーを昇順へ切り替える | 作成日時の昇順に行を並べ、その列の昇順三角を表示する |
| WS-070 | 作成日時ヘッダーを降順へ切り替える | 作成日時の降順に行を並べ、その列の降順三角を表示する |
| WS-071 | 更新日時ヘッダーを昇順へ切り替える | 更新日時の昇順に行を並べ、その列の昇順三角を表示する |
| WS-072 | 更新日時ヘッダーを降順へ切り替える | 更新日時の降順に行を並べ、その列の降順三角を表示する |
| WS-073 | PJの作業を閉じる | 画像一覧を閉じ、PJと画像データを保持する |
| WS-074 | 閉じたPJを開く | 画像一覧・候補・手描き・確認状態・非表示を復元する |
| WS-075 | サーバー再起動後にPJを開く | 保存済み状態を復元し、別PJのデータを混ぜない |
| WS-076 | PJ完了を取消 | 作業中のまま画像一覧を保持する |
| WS-077 | PJ完了を確定 | 一覧を閉じ、PJ管理に完了と表示する。画像データは保持する |
| WS-078 | 完了PJを開く | 閲覧・移動・範囲ダウンロードは有効、描画・検出・保存・Undoは無効 |
| WS-079 | 完了PJで作業を再開 | 作業中になり、候補と描画の操作を再有効化する |
| WS-080 | PJ削除の確認を開く | 対象名と履歴・モザイク・除外削除、元画像保持、取消不能を明記する |
| WS-081 | PJ削除を取消 | 対象PJを保持する |
| WS-082 | 別PJの削除を確定 | そのPJだけ消え、現在のPJと編集は保持する |
| WS-083 | 現在PJの削除を確定 | 現在PJと関連データが消え、空の作業画面へ戻る |
| WS-084 | 同じフォルダーを別PJで読み込む | 既存PJを示す警告と、既存PJを開く・別に作成の選択肢を表示する |
| WS-085 | 同フォルダー警告で既存PJを開く | 選択PJを開き、新規PJを増やさない |
| WS-086 | 同フォルダー警告で別に作成 | 既存PJを変更せず、別PJとして作業できる |
| WS-087 | 同フォルダー警告を取消 | 読み込み先PJと画像一覧を変えない |
| WS-088 | 元画像の更新時刻を変更してPJを開く | 変更したファイルだけを警告一覧へ表示する |
| WS-089 | 変更警告で範囲削除を未チェックで確定 | 対象の編集を保持し、確認済みの更新時刻へ更新する |
| WS-090 | 変更警告で範囲削除をチェックして確定 | 変更対象のモザイク・除外を削除し、他画像は保持する |
| WS-091 | 寸法変更画像で範囲削除を未チェック | 新寸法に合わせて編集を保持し、対象画像の古いUndo履歴をリセットする |
| WS-092 | 寸法変更画像で範囲削除をチェック | 対象画像の範囲と履歴を削除する |
| WS-093 | 不足フォルダーを含むPJを開き、表示復帰または別タブ切替後に権威一覧を再取得する | 利用可能画像は表示し、不足したnative-folderだけの再指定を案内する。再取得後もPJ・画像・source一覧と不足表示が一致する |
| WS-094 | browser画像を含むPJで不足フォルダーの再指定画面からnative sourceを1件選び、移動先パスを入力して確定する。続けてGPUキャッシュ更新を失敗させ、同じ再指定をやり直す | 失敗時は関連付け前の元フォルダー・変換状態・画面を保持し、再試行だけ新しいパスへ切り替える。成功時は指定した元フォルダーだけ関連付け直し、browser画像の候補・手描き・反転・確認状態と残りの不足を保持する。OSのフォルダー選択は使わない |
| WS-096 | 一覧から削除した画像のあるPJを開き直す | 元フォルダーにファイルがあっても削除済みのPJ画像を復活させない |
| WS-097 | 画像一覧のクリアを取消 | 一覧とPJデータを保持する |
| WS-098 | 画像一覧のクリアを確定 | 画像データを消してPJを閉じる。PJ自体は空で残り、元ファイルは残る |
| WS-099 | PJ Aを開いたまま、PJ Bを開く・完了PJを再開して開く・PJを閉じる・新規PJ作成を連続して操作する。各操作中は戻る操作、読込、PJ一覧の操作も試す | 操作中のPJ操作は重複開始できない。完了PJの再開と開くは1要求で完了し、失敗時に完了状態だけが作業中へ残らない。最後に表示したPJまたは無名作業だけが一覧と編集対象に残る |
| WS-100 | フォルダーAの読込をNetworkで遅延させ、完了前に元PJで確認済み・非表示・反転・手描きを行う。境界ツールを選んだままキャンバスを操作し、作成済みの境界ドラフトも残す。PJ Bを開く、フォルダーCを読む、検出・保存・マスククリア・画像削除を試し、タブを非表示にしてから復帰する | 読込中は確認済み・非表示・反転・手描きだけ保存できる。構造変更と新しい境界操作は開始できず、作成済みの境界ドラフトは残る。2件目の遷移も開始しない。最初の読込だけ完了し、元PJを開き直すと許可した編集を復元する。`/api/images` の catalogGeneration が変化した復帰時は権威スナップショットへ安全に置換し、PJ・件数・画像がサーバーと一致する |
| WS-101 | ブラウザーから複数file sourceとdirectory sourceを追加したPJを閉じて再開する。許可済みのsourceは自動復元を確認し、未許可のsourceごとにPJ画面の「復元」を押して許可・拒否を分ける。表示復帰または別タブPJ切替後に同じPJの復元を重ねて開始しても確認する | 許可済みのfile sourceとdirectory sourceは自動で復元する。権威一覧への置換後も現PJの画像だけへ保存済みhandleとサイズ・更新時刻を対応付ける。最後に開始した復元だけが現PJのaccessと不足source表示を公開する。未許可・拒否・列挙失敗したsourceだけをPJ画面に列挙し、各「復元」操作から許可を求める。別PJのhandle・候補を混ぜない |
| WS-102 | 現在PJへの画像追加または検出中に、管理表から同じPJの削除を確定する | 削除を拒否し、PJ・画像・候補・履歴を保持する。処理完了後に削除すると対象PJだけを削除できる |
| WS-103 | 完了操作と検出開始を同時に繰り返す | 完了したPJは必ず閲覧専用となり、検出開始と編集可能状態が残らない |
| WS-104 | 完了PJを「再開して開く」で開く途中に、利用可能な元フォルダーを無効なパスへ変更する | 開く失敗後もPJは完了のままで、直前に開いていたPJの一覧・候補・編集状態を保持する |
| WS-105 | `G:\\Pics` を読み込んでPJを作り、同じ場所を `g:\\pics` として追加・再指定する | 元フォルダーを1つとして扱い、同じ画像を重複表示・重複保存しない |
| WS-106 | 複数の元フォルダーを持つPJを開く際、後半のフォルダーを読めない状態にする | 開く操作は失敗し、直前のPJの一覧・候補・編集状態を保持する。開こうとしたPJの元フォルダー記録、画像の確認済み状態、反転状態、PJ状態は変更しない |
| WS-107 | 保存済み候補が不正なPJまたは通常のフォルダーを読み込む。PJの元フォルダー再指定でも同じ状態にする | 読込・再指定の失敗でも現在の一覧・候補・catalogGenerationを保持し、途中まで読んだPJや無名一覧を表示せず、再指定先のnative path・変換状態を保存しない |
| WS-108 | PJのブラウザー画像を単体・複数選択で削除し、単体・一括コピー保存の「コピー後に元画像を削除」でも成功応答を失わせてからMozarieを再起動する。削除中に別タブで起動時照合も行う | 再起動時に権威PJ状態で削除済みと分かれば、その画像だけの保存済みハンドルを削除する。存在中・削除が拒否・未確定なら記録とハンドルを残し、明確な拒否を受けた削除要求だけ記録を消す |
| WS-109 | 両画面の「絞り込み」をEnterまたはSpaceで開き、複数項目を選ぶ | チェック項目は複数選べ、ボタンに選択数を表示する。確認済みにして次へは左一覧で選んだ対象順のまま移動する |
| WS-110 | 完了PJで両画面の「絞り込み」を開いて選択を変える。通常PJで開いたまま読込・検出・保存を開始し、完了後にもう一度開く | 完了PJでも絞り込みボタンとチェック項目は使える。処理の開始時は開いていたメニューが閉じ、ボタンと項目が無効になる。処理完了後は再び開いて選択を変えられる |
| WS-111 | パス入力で対応画像0件のフォルダーを読み込む | 対応画像がないことを案内し、直前の一覧・選択・PJ画像・カタログ世代を保持する。CMDにはフォルダー走査の開始・完了、候補0件・読込0件・スキップなしを表示する |
| WS-112 | PNG・JPEG・WebPと壊れた画像を混在させたフォルダーをパス入力で読み込む | 正常画像だけを一覧とサムネイルへ読み込み、壊れた画像だけをスキップする。CMDの走査完了は候補数・読込数・理由別スキップ数・代表相対名・所要時間をまとめて表示し、1ファイルごとの大量ログを出さない |
| WS-113 | 大きいzTXtまたはiTXtを持つPNGを、通常画像と一緒にパス入力で読み込む。CRC不一致と終端前で切れたPNGも混ぜる | 正常PNGは画像本体と一覧へ読み込み、不要な文字列メタデータを展開しない。CRC不一致と切れたPNGだけを安全にスキップし、他の画像を読み込む |
| WS-114 | ブラウザーの「フォルダーを選択」で許可済みの画像フォルダーを追加する | 読み込み権限だけを要求し、配下の対応画像を追加する。完了・失敗・取消のいずれでも読み込み状態が残らず、次のパス入力または画像追加を開始できる |
| WS-115 | ブラウザーのフォルダー選択を取消し、権限を拒否した場合も確認する | 取消または選択失敗の状態を画面に表示し、一覧・選択・読み込み状態を変えない。例外種別だけで権限拒否を無反応にしない |
| WS-116 | 画像追加、フォルダー読込、またはカタログ切替中にパス入力読込とブラウザーのフォルダー選択を開始する | 新しい読込を開始せず、「画像の読み込みまたは処理が完了してから」再実行する状態を表示する。先行読込・一覧・PJ状態を保持する |
| WS-117 | パス読込、PJ作成・切替・削除、状態一括変更、検出、保存、設定保存、モデルダウンロード、更新開始を各1回行い、CMDを確認する | 各高位操作で日本語の操作名・IDを含まないAPIルート・開始/完了または失敗・status・error_code・所要時間を表示する。対象数と元フォルダーパスがある操作は表示し、本文・トークン・ヘッダー・画像IDは表示しない |
| WS-118 | 検出または保存を開始して、一時停止・再開・キャンセル・完了・失敗を確認する。ブラウザー画像を複数追加し、ブラウザー保存も実行する | CMDに処理名・対象数、pause/resume/cancel受付、完了数・所要時間、失敗時のerror_codeを表示する。画像追加はセッション開始・完了、保存は一括操作の開始・完了だけをINFOに集約し、個別の`/api/import/file`、reserve/render/commit/ack成功、ジョブ取得、サムネイル、候補ON/OFF、手描き自動保存はINFOログを大量に出さない。個別失敗はID・本文・token・headerを出さずに警告できる |
| WS-119 | 対応拡張子だがすべて壊れた画像だけのフォルダーをパス入力で読み込む | 読み込めないことを案内し、直前の一覧・選択・PJ画像・カタログ世代を保持する。CMDの走査完了に候補数・読込0件・理由別スキップ数を表示する |
| WS-120 | 無名でフォルダーAを読み込み、候補のON/OFF・削除・枠、手描きモザイク・除外、確認済み・非表示、反転、範囲クリアを各1回実行してCtrl+Z→Ctrl+Shift+Zを行う | すべてが1操作ずつの永続履歴で戻り、やり直せる。対象外画像の状態は変わらない |
| WS-121 | WS-120の無名作業をサーバー再起動後に同じフォルダーAのパス入力で読み込む | 同じ画像ID、候補、手描き、確認済み・非表示、反転、Undo/Redo位置を復元する。PJ一覧には無名作業を表示しない |
| WS-122 | WS-121の無名作業を名前保存する | 画面を置き換えず、同じ画像IDと履歴のまま名前付きPJになる。名前保存前のUndo/Redoも続けて使える |
| WS-123 | 無名フォルダーAを編集した後、別フォルダーBをパス入力で読み込む。Bの読込を失敗させた場合も別々に行う | Bが完全に読めた時だけBの無名作業へ切り替え、Aの内部作業を破棄する。Bの読込失敗時はAの画像・候補・履歴・選択を保持する |
| WS-124 | 無名の複数画像で全画像・選択画像・現在画像の自動検出をそれぞれ開始し、成功・取消・1件失敗・フォルダー再読込を行う | 成功時だけ対象全件の候補と履歴を1回のSQLiteトランザクション・1グループとして公開する。取消・失敗・再読込では対象全件の候補、確認済み、画像ID、履歴を開始前のまま保持する |
| WS-125 | 無名状態で新規フォルダー読込またはブラウザー画像読込を成功・失敗・取消で行い、名前保存・新規PJ・閉じる・再起動を確認する | 成功時だけ作業用workspace、source、画像とactive情報を公開する。失敗・取消で内部workspaceを残さず、名前保存では同じworkspace IDへブラウザーhandleを保持する。無名作業を閉じるか別PJへ切り替えた後は再起動しても復活しない |
| WS-126 | 無名フォルダーAを編集後、別フォルダーBを読み込む。Bの公開処理で失敗した場合も確認し、再起動後に同じ操作を行う | Bの作業を公開できた時だけactive情報、一覧、候補、履歴をBへ切り替え、Aの内部workspaceを破棄する。公開に失敗した場合はAの一覧・候補・履歴とactive情報を保持し、Bの内部workspaceを残さない |
| WS-129 | Pillow の既定ピクセル数を超えるPNG・JPEG・WebPを通常画像と一緒にパス入力で読み込む。Orientationが90度回転する巨大JPEGも含め、同時に2件の巨大画像をサムネイル表示する | 3形式とも一覧へ入り、巨大JPEGの記録寸法・サムネイル・編集座標が回転後の向きで一致する。同時表示中もPillowの画素数制限警告を出さず、完了後は別画像を選択・表示できる |
| WS-130 | メモリ不足になる大きな画像を、通常画像と一緒にパス入力で読み込む | 読み込み時はその画像だけをCMDの理由別スキップへ記録し、通常画像を一覧へ残す |
| WS-131 | 読込後にメモリ不足になる大きな画像のサムネイルまたは表示を開く | 対象画像の読み込み失敗を画面に示し、サーバーを停止させない。通常画像の表示を続けられる |
| WS-132 | 読込後にメモリ不足になる大きな画像を自動検出する | 対象画像の読み込み失敗を画面に示し、サーバーを停止させない。失敗後も新しい検出を開始できる |
| WS-133 | 読込後にメモリ不足になる大きな画像で、手描きマスクまたは候補マスクを含めてブラウザー保存する | `internal_error`ではなく画像またはマスクの読み込み失敗を画面に示し、サーバーを停止させない。失敗後も通常画像を保存できる |
| WS-134 | 読込後にメモリ不足になる大きな画像をコピー保存またはバックグラウンド保存する | 対象画像の読み込み失敗を画面に示し、サーバーを停止させない。失敗後も新しい保存を開始できる |
| WS-135 | ブラウザーから11件以上の画像を追加し、画像追加workerを11に設定してNetworkを確認する | 実効並列数は対象数までで、11は10へ丸められず利用者設定より小さい固定上限を追加しない。同じsessionの同時転送数は11を超えず、全要求が終了後に転送状態を残さない |
| WS-136 | 仮想ギャラリーの表示範囲で異なる画像のサムネイルを同時に出し、同じ画像・版の要求も重ねる | 表示範囲の異なる画像は固定の全体worker枠で待たず、同じ画像・版だけは画像I/Oロックで1回だけ生成する。完了後に待機ロックが画像数に比例して残らない |
| WS-137 | 画像追加workerを11にして追加中にNetworkを中断・再試行し、別origin・無効session・認証なしの転送も確認する | 通常のブラウザー追加は利用者設定の実効並列数で送信し、サーバーは固定の転送枠を追加しない。session・origin・認証の検証は維持し、失敗・中断した転送も終了処理で一覧とPJ状態を壊さず転送状態を残さない |
| WS-138 | 実在する絶対パスをフォルダー入力で読み込み、ブラウザーdirectory sourceも併用する | CIで設定値の相対形式を拒否する。実機では成功時の正規化済み絶対形式を入力欄へ反映し、directory handleの表示名やImageRecordの相対名を絶対パス扱いしないことを確認する。 |
| WS-139 | 5,000件以上と壊れた画像を混在させたフォルダーをパス入力で読み込む。走査中に現在のPJで確認済み・候補編集を行い、別ケースではMozarieを終了する | 正常画像は全件を相対名順で一覧へ公開し、壊れた画像だけを理由付きで案内する。走査中の既存PJ操作は待たずに確定する。終了時は途中の新規一覧を公開せず、次回起動後に新しい走査workerを残さない。 |
