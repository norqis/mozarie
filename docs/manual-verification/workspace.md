# 起動・読み込み・一覧・プロジェクト

[共通確認](../manual-verification.md)を全行で実施する。

自動テストで再現できない外部環境の観測だけを残す。決定的な規則は `tests/verification-contracts.workspace.json` から実行中のテストへ対応付ける。実ブラウザーやOSが所有する許可UIは指定した実環境で確認し、実メモリ不足は安全に再現できる隔離実機でのみ確認する。

| ID | 手順 | 期待値 |
| --- | --- | --- |
| WS-143.4 | Chromium File System Access許可UIと実階層フォルダー 書込み許可プロンプトとOS所有の親子directory handle削除先はheadless擬似handleで完全には観測できない。 | 親・子・孫の実フォルダーで初回許可、拒否、復元、取消、孫だけの削除・保存を確認する。 |
| WS-001.2 | 新規Windows環境と実ネットワーク 未導入のPython・pip・GPU依存を実際に取得する初回setupの所要と外部インストーラー結果は隔離CIで固定できない。 | 新規展開先でsetup.batを実行し、外部依存の導入と完了案内を確認する。 |
| WS-002.2 | 依存導入済みWindows環境 既存仮想環境・pipキャッシュ・実プロセスとの組合せは隔離fixtureで忠実に再現できない。 | 同じ展開先でsetup.batを再実行し、既存環境を壊さず完了することを確認する。 |
| WS-003.2 | Windowsデスクトップと既定ブラウザー 既定ブラウザーの起動と実ウィンドウ表示はheadless CIから観測できない。 | run.batから実ブラウザーが開き、ページへ接続できることを確認する。 |
| WS-094.4 | Windows上のアクセス不能な実フォルダー ACL・別プロセスによるアクセス拒否とOSファイルキャッシュの実挙動は一時fixtureだけでは再現できない。 | 不足native sourceを実フォルダーへ再指定し、アクセス拒否後の再試行を確認する。 |
| WS-101.6 | Chromiumの永続プロファイルとFile System Access許可UI ブラウザー再起動をまたぐhandle許可状態とユーザー許可UIはheadless fixtureで制御できない。 | file sourceとdirectory sourceの許可済み自動復元、拒否、明示復元を実ブラウザーで確認する。 |
| WS-108.3 | Chromium File System Accessと実フォルダー OSが管理するhandleの削除権限とブラウザー再起動後の許可状態はCIの擬似handleでは再現できない。 | 実フォルダーのbrowser画像を削除・コピー後削除し、再起動後のhandle整理を確認する。 |
| WS-114.3 | ChromiumのFile System Access許可UI showDirectoryPickerの実許可プロンプトは自動化APIから操作・観測できない。 | 実ブラウザーでフォルダー選択時に書き込み権限が要求されることを確認する。 |
| WS-115.3 | ChromiumのFile System Access許可UI OS・ブラウザー所有の拒否表示と許可状態はheadless fixtureで再現できない。 | 実ブラウザーで選択取消と権限拒否を行い、案内と一覧保持を確認する。 |
| WS-129.1 | 十分なRAMを持つWindows実機と巨大PNG/JPEG/WebP・回転JPEG 3形式の既定ピクセル数超過画像を同時に一覧・編集表示した座標とサムネイルの一致はCIの小fixtureでは実メモリ・デコード経路を再現できない。 | 3形式とも一覧へ入り、巨大JPEGの記録寸法・サムネイル・編集座標が回転後の向きで一致する。 |
| WS-129.2 | 十分なRAMを持つWindows実機と巨大PNG/JPEG/WebP Pillow既定上限超過を警告なしで表示し続けながら別画像へ切り替える応答性はCI runnerの実メモリ量に依存する。 | 同時表示中もPillowの画素数制限警告を出さず、完了後は別画像を選択・表示できる |
| WS-129.3 | 十分なRAMを持つWindows実機と巨大PNG/JPEG/WebP Pillow既定上限超過画像を複数同時表示した実メモリ圧と描画応答はCI資源量に依存する。 | 既定ピクセル数超過の3形式と回転JPEGを同時表示し、応答性を確認する。 |
| WS-130.2 | Windows 11実機。利用可能RAMを意図的に圧迫し、数千画像を含む実フォルダーを読み込む。 OS全体の実メモリ枯渇とプロセス強制回収はCIで安全かつ決定的に再現できないため。 | 読込走査中の実メモリ不足で対象だけがスキップされることを確認する。 |
| WS-131.3 | Windows 11実機。画像表示中に実メモリ不足を発生させ、解放後に別の通常画像を開く。 画像デコーダーとOSの実メモリ割当失敗はCIで安全かつ決定的に再現できないため。 | 表示時の実メモリ不足後も通常画像を操作できることを確認する。 |
| WS-132.3 | Windows 11実機。GPUまたはCPU検出中に実メモリ不足を発生させ、解放後に次の検出を開始する。 推論ランタイムを含む実メモリ枯渇はCIで安全かつ決定的に再現できないため。 | 自動検出中の実メモリ不足後も次の検出を開始できることを確認する。 |
| WS-133.3 | Windows 11実機。手描きまたは候補マスク付き画像をブラウザー保存中に実メモリ不足にし、解放後に再保存する。 ブラウザーとOSを跨ぐ実メモリ枯渇はCIで安全かつ決定的に再現できないため。 | 手描きまたは候補マスク付きブラウザー保存の実メモリ不足後に再保存できることを確認する。 |
| WS-134.3 | Windows 11実機。コピー保存またはバックグラウンド保存中に実メモリ不足にし、解放後に再保存する。 ファイルI/OとOS実メモリ枯渇の組合せはCIで安全かつ決定的に再現できないため。 | コピーまたはバックグラウンド保存の実メモリ不足後に再保存できることを確認する。 |
| WS-138.3 | Windowsの実在絶対パスとChromium directory handle OSが返す正規化済みドライブパス表示とdirectory handle表示名の区別は非Windows/headless CIでは同じ形で観測できない。 | 実在絶対パスとbrowser directory sourceを併用し、入力欄と表示名を確認する。 |
| WS-139.4 | Windowsで5,000件以上の実画像とファイル共有 大規模実ディスクI/O、別プロセスの共有ロック、アプリ終了の組合せは隔離CIの小fixtureで実負荷を再現できない。 | 5,000件以上の実フォルダー走査中に編集と終了を行い、応答と再起動後を確認する。 |
