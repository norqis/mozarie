# 起動・読み込み・一覧・プロジェクト

[共通確認](../manual-verification.md)を全行で実施する。

自動テストで再現できない外部環境の観測だけを残す。決定的な規則は `tests/verification-contracts.workspace.json` から実行中のテストへ対応付ける。Windowsダイアログの外観・操作だけを確認する項目は廃止した。

| ID | 手順 | 期待値 |
| --- | --- | --- |
| WS-143.4 | Chromium File System Access許可UIと実階層フォルダーで実施する。書込み許可プロンプトとOS所有の親子directory handle削除先はheadless擬似handleで完全には観測できない。 | 親・子・孫の実フォルダーで初回許可、拒否、復元、取消、孫だけの削除・保存を確認する。 |
| WS-001.2 | 新規Windows環境と実ネットワークで実施する。未導入のPython・pip・GPU依存を実際に取得する初回setupの所要と外部インストーラー結果は隔離CIで固定できない。 | 新規展開先でsetup.batを実行し、外部依存の導入と完了案内を確認する。 |
| WS-002.2 | 依存導入済みWindows環境で実施する。既存仮想環境・pipキャッシュ・実プロセスとの組合せは隔離fixtureで忠実に再現できない。 | 同じ展開先でsetup.batを再実行し、既存環境を壊さず完了することを確認する。 |
| WS-094.4 | Windows上のアクセス不能な実フォルダーで実施する。ACL・別プロセスによるアクセス拒否とOSファイルキャッシュの実挙動は一時fixtureだけでは再現できない。 | 不足native sourceを実フォルダーへ再指定し、アクセス拒否後の再試行を確認する。 |
| WS-108.3 | Chromium File System Accessと実フォルダーで実施する。OSが管理するhandleの削除権限とブラウザー再起動後の許可状態はCIの擬似handleでは再現できない。 | 実フォルダーのbrowser画像を削除・コピー後削除し、再起動後のhandle整理を確認する。 |
| WS-129.1 | 十分なRAMを持つWindows実機と巨大PNG/JPEG/WebP・回転JPEGで実施する。3形式の既定ピクセル数超過画像を同時に一覧・編集表示した座標とサムネイルの一致はCIの小fixtureでは実メモリ・デコード経路を再現できない。 | 3形式とも一覧へ入り、巨大JPEGの記録寸法・サムネイル・編集座標が回転後の向きで一致する。 |
| WS-129.2 | 十分なRAMを持つWindows実機と巨大PNG/JPEG/WebPで実施する。Pillow既定上限超過を警告なしで表示し続けながら別画像へ切り替える応答性はCI runnerの実メモリ量に依存する。 | 同時表示中もPillowの画素数制限警告を出さず、完了後は別画像を選択・表示できる |
| WS-129.3 | 十分なRAMを持つWindows実機と巨大PNG/JPEG/WebPで実施する。Pillow既定上限超過画像を複数同時表示した実メモリ圧と描画応答はCI資源量に依存する。 | 既定ピクセル数超過の3形式と回転JPEGを同時表示し、応答性を確認する。 |
| WS-130.2 | 制限された実メモリ環境で実施する。実OSのメモリ不足発生点とプロセス生存性は安全かつ決定的にCIで作れない。 | 読込走査中の実メモリ不足で対象だけがスキップされることを確認する。 |
| WS-131.3 | 制限された実メモリ環境で実施する。サムネイル・表示デコードでの実OSメモリ不足はCI資源量に依存する。 | 表示時の実メモリ不足後も通常画像を操作できることを確認する。 |
| WS-132.3 | 実モデルを導入した制限メモリ環境で実施する。推論中の実CPU/GPUメモリ不足はモデル・ドライバー・搭載量に依存する。 | 自動検出中の実メモリ不足後も次の検出を開始できることを確認する。 |
| WS-133.3 | Chromium File System Accessと制限メモリ環境で実施する。ブラウザー保存と実OSメモリ不足の組合せはheadless fixtureで決定的に再現できない。 | 手描きまたは候補マスク付きブラウザー保存の実メモリ不足後に再保存できることを確認する。 |
| WS-134.3 | 制限された実メモリ環境で実施する。バックグラウンド保存中の実OSメモリ不足はCI資源量に依存する。 | コピーまたはバックグラウンド保存の実メモリ不足後に再保存できることを確認する。 |
| WS-138.3 | Windowsの実在絶対パスとChromium directory handleで実施する。OSが返す正規化済みドライブパス表示とdirectory handle表示名の区別は非Windows/headless CIでは同じ形で観測できない。 | 実在絶対パスとbrowser directory sourceを併用し、入力欄と表示名を確認する。 |
| WS-139.4 | Windowsで5,000件以上の実画像とファイル共有で実施する。大規模実ディスクI/O、別プロセスの共有ロック、アプリ終了の組合せは隔離CIの小fixtureで実負荷を再現できない。 | 5,000件以上の実フォルダー走査中に編集と終了を行い、応答と再起動後を確認する。 |
