# 通信・対象の組合せ・PJデータ照合

[共通確認](../manual-verification.md)を全行で実施する。OS選択ダイアログは開かない。

## 対象の準備

A～Hは確認用画像を区別する記号。ファイル名は任意とし、各記号と実ファイル名を対応させる。パス入力で読み込み、名前付き・作業中PJとして保存する。別PJには別の画像と編集を用意する。

| 画像 | モザイク | 確認状態 | 表示状態 |
| --- | --- | --- | --- |
| A | あり | 確認済み | 表示 |
| B | あり | 未確認 | 表示 |
| C | なし | 確認済み | 表示 |
| D | なし | 未確認 | 表示 |
| E | あり | 確認済み | 非表示 |
| F | あり | 未確認 | 非表示 |
| G | なし | 確認済み | 非表示 |
| H | なし | 未確認 | 非表示 |

モザイクありは有効な範囲が残る状態にする。保存比較には各画像の未加工コピーを別に保持する。上書き確認は確認用画像だけで行う。

## 画像・通信

| ID | 手順 | 期待値 |
| --- | --- | --- |
| DI-001 | Networkのキャッシュ無効をONにし、パス入力からA～Hを読み込む | 画像一覧APIに8画像が含まれ、名前・寸法・IDの対応が一覧と一致する |
| DI-002 | 左一覧を先頭から末尾までスクロールし、表示した各サムネイルの要求とPreviewを見る | 各`/api/thumbnail/`が200で画像を返し、対応する絵柄を表示する。壊れた画像アイコン、空欄、404はない |
| DI-003 | A～Hを1枚ずつ選択し、原画像・候補・マスクの要求を確認 | 各`/api/image/`が200で開ける画像を返す。候補がある場合は候補とマスクのIDが選択画像に属する。手描きだけの場合に候補マスク要求を必須としない |
| DI-004 | キャッシュ無効をOFFにして同じ画像を再選択 | 通信成功・ブラウザーキャッシュ・アプリ内キャッシュのいずれかで画像を表示する。キャッシュ利用を未取得エラーと扱わない |
| DI-005 | 画像Aを編集してBへ移動し、Aへ戻る | 新しい候補・マスクの版を表示し、古いキャッシュで編集前へ戻らない |
| DI-006 | 400画像の全サムネイルを順に表示する | 通過した400画像のサムネイルが表示される。末尾到達だけで未通過の画像を確認済みとしない |
| DI-007 | 400画像の一覧を往復しNetworkの原画像要求を見る | 一覧スクロールだけで400件の原画像を一斉取得しない。サムネイルと選択・近傍画像の取得を区別する |
| DI-008 | 読み込み後に確認用画像を削除し、ページを再読み込みして対象画像を選ぶ | 対象ファイルの不足を案内し、直前の画像を削除画像として表示しない。失敗要求が再試行で増え続けない |
| DI-009 | 存在する画像を変更せずPJ再開・ページ再読み込みを行う | サムネイルと原画像を再表示でき、「元画像が変わりました」を出さない |
| DI-258 | 通常画像と、ヘッダ検証は通るが画素展開に失敗するPNGを同じフォルダーから読み込む | 壊れた画像はファイル名と理由を一覧で案内し、通常画像は一覧・サムネイル・編集画面へ読み込み続ける。壊れた画像をサムネイルまたは編集表示で初めて失敗させない |
| DI-009a | 読み込めるPNG、破損PNG、読み込めるPNGをこの順にブラウザーの画像選択またはドロップで追加する | 前後の正常画像は追加され、進捗は3件で完了する。読み込めなかった画像ダイアログに破損ファイルの相対名と理由が全件表示され、汎用エラーダイアログは出ない。 |
| DI-009b | 破損PNGだけをブラウザーで追加し、別ケースで正常PNGと破損PNGが混在するネイティブのフォルダーパスを読み込む | 正常ファイルは公開を続行する。全件破損時もファイル名と理由の全件一覧を表示し、既存一覧を不透明な失敗で置換しない。CMDには選択したルートと各相対名・理由が残る。 |
| DI-009c | Networkで任意の`/api/thumbnail/`を一度500にし、カードに出た再試行表示を押す。別カードと原画像も続けて開く | 失敗カードだけが再試行可能な表示になり、再試行後は画像を表示する。他カードと次の正常な原画像の選択は止まらない。 |

## 一覧と保存対象

左一覧と一覧表示の両方でDI-010～015を別々に実施する。検索は空、フォルダーは全体とする。

| ID | 手順 | 期待値 |
| --- | --- | --- |
| DI-010 | 一覧を「すべて」にする | A～Hの8画像を表示する |
| DI-011 | 一覧を「モザイクあり」にする | A・Bの2画像だけ表示する |
| DI-012 | 一覧を「モザイク無し」にする | C・Dの2画像だけ表示する |
| DI-013 | 一覧を「確認済み」にする | A・Cの2画像だけ表示する |
| DI-014 | 一覧を「未確認」にする | B・Dの2画像だけ表示する |
| DI-015 | 一覧を「非表示」にする | E・F・G・Hの4画像だけ表示する |
| DI-016 | 一括保存対象を「画像一覧のすべて」にして上書き保存する | 対象4件でA～Dだけを保存する。非表示のE～Hは含めない |
| DI-017 | 一括保存対象を「モザイクあり」にして上書き保存する | 対象2件でA・Bだけ保存し、C～Hを変更しない |
| DI-018 | 一括保存対象を「確認済み」にして上書き保存する | 対象2件でA・Cだけ保存し、B・D・E～Hを変更しない |
| DI-019 | 「画像一覧のすべて」で保存済みのA～Dを再保存する | 再び対象4件となり、A～Dだけを処理する |
| DI-020 | 「モザイクあり」で保存済みのA・Bを再保存する | 再び対象2件となり、A・Bだけを処理する |
| DI-021 | 「確認済み」で保存済みのA・Cを再保存する | 再び対象2件となり、A・Cだけを処理する |
| DI-022 | 左一覧を「未確認」に絞って一括保存対象を「すべて」にする | 左の表示2件ではなく、非表示を除くPJ全体A～Dを対象とする |
| DI-023 | 一覧表示でAだけを検索し、一括保存対象を「すべて」にする | 検索結果1件ではなく、非表示を除くPJ全体A～Dを対象とする |
| DI-024 | Aを選び単一ファイル保存で上書きする | Aだけ保存し、B～Hの内容を変更しない。保存対象プルダウンを表示しない |
| DI-025 | 一括保存の完了後にPJを閉じて開く | A～Hのモザイク・確認状態・非表示が準備時の組合せで残り、候補や画像を削除しない |
| DI-026 | モザイクなしの画像だけのPJで一括保存を開き「モザイクあり」を選ぶ | 0件と表示して開始を無効にし、「すべて」へ切り替えると保存できる |
| DI-027 | 未確認画像だけのPJで一括保存の「確認済み」を選ぶ | 0件と表示して開始を無効にし、確認済み画像を作った後はその画像だけを対象にできる |
| DI-028 | Bのモザイクを全消去し、一括保存の「モザイクあり」を選ぶ | 対象がAの1件になる |
| DI-029 | Bのモザイク全消去をUndoし、一括保存の「モザイクあり」を選ぶ | 対象がA・Bの2件に戻る |

## PJ・履歴・書き出し

| ID | 手順 | 期待値 |
| --- | --- | --- |
| DI-030 | Aにモザイク、Bに除外を描き、AでUndoする | Aの直前描画だけ戻り、Bの除外とBのUndo位置を保持する |
| DI-031 | 別PJへ切り替えてUndoする | 開いたPJの現在画像だけを戻し、元PJの候補・範囲・履歴位置を変更しない |
| DI-032 | PJ完了後に再開しA～Hを選ぶ | 8画像の候補・枠・ON/OFF・手描き・確認済み・非表示を保持する |
| DI-033 | PJを閉じた状態でサーバーを再起動し、PJ管理から開く | A～Hを追加読込せず復元し、各画像の範囲と履歴を使える |
| DI-034 | Aの削除を確定後、PJを開き直す | Aが一覧から消え、Aの候補・手描き・履歴を操作できない。B～Hと元ファイルAは残る |
| DI-035 | 確認用PJを削除し、専用DBを読み取り専用で確認する | 対象PJの画像・元フォルダー関連・候補・手描き・候補枠・履歴・履歴参照が残らない。別PJの行と元画像は残る |
| DI-036 | モザイクと除外が重なる画像のモザイク範囲PNGを保存して開く | 元画像寸法のマスクで、除外と重なる部分は適用範囲に含まれない |
| DI-037 | 除外範囲PNGを保存して開く | 元画像寸法で、有効な除外候補と手描き除外を合成し、除外消しゴムの部分を差し引いたマスクになる |
| DI-038 | 非表示画像を含むPJのモザイクZIPを保存して展開する | 表示画像に対応するPNGだけが揃い、非表示画像のPNGを含めない。各寸法と除外差引き後の範囲が単体出力と一致する |
| DI-039 | 非表示画像を含むPJの除外ZIPを保存して展開する | 表示画像に対応するPNGだけが揃い、非表示画像のPNGを含めない。各寸法と除外範囲が単体出力と一致する |
| DI-040 | 別フォルダーの同名画像を含むPJをZIP出力する | 両画像のマスクが衝突せずZIPに残り、各元画像との対応を識別できる |
| DI-041 | 候補OFF後に画像とマスクを保存する | OFF候補を加工・適用範囲へ含めず、ON候補の範囲は残る |
| DI-042 | 除外ON後に画像とマスクを保存する | 除外部分の元画像を保持し、モザイクPNGからもその部分を差し引く |
| DI-043 | 枠0pxと10pxで同じ候補のマスクを保存する | 10px側は元候補の周囲10pxを拡張し、画像端より外の画素を作らない |
| DI-044 | PNG・JPEG・WebPを含むPJを上書き保存し、各出力を開く | ファイルごとの形式と寸法を維持し、拡張子だけ別形式にしない |

## 一覧幅・表示領域

| ID | 手順 | 期待値 |
| --- | --- | --- |
| DI-045 | 左一覧を1列になる幅へドラッグ | ファイル名と寸法の欄がカード下端内に収まり、画像に押し出されない |
| DI-046 | 左一覧を216px幅へドラッグ | 144px高の各カード内にサムネイル・確認状態・ファイル名・寸法が表示される |
| DI-047 | 左一覧を2列へ切り替わる直前まで広げる | 最も広い1列カードでもファイル名と寸法を下へ切らない |
| DI-048 | 左一覧を2列へ切り替わるまで広げる | 各カードの名前・寸法を表示し、列や次の行へ重ならない |
| DI-049 | 左一覧を216px幅にしてページを再読み込み | 幅が復元され、再操作しなくても最初から名前・寸法を表示する |
| DI-050 | 1列一覧を末尾へスクロール | 各カードの名前・寸法が隠れず、行間隔とスクロール位置が画像取得で跳ねない |

## 一括操作・状態復元

| ID | 手順 | 期待値 |
| --- | --- | --- |
| DI-051 | A・Bを選択して一括モザイククリアを確定 | A・Bの候補・手描きモザイク・除外・除外消しゴムを消し、両画像を未確認にする。表示状態とE・Fの全状態を保持する |
| DI-052 | A・Bの一括クリア後、AでUndo | A・Bの範囲を復元し、Aは確認済み・Bは未確認に戻る。両画像の表示状態と対象外を変えない |
| DI-053 | A・Bの一括クリアをUndoした後、AでRedo | A・Bだけを再びクリアして未確認にする。表示状態と対象外を変えない |
| DI-054 | A・Bを選択して一括自動検出を開く | 設定の対象2件となり、C～Hを含めない。開始前は検出しない |
| DI-055 | Aの確認済み切替後にUndo | Aの確認状態だけを切替前に戻す |
| DI-056 | Aを非表示にしてから再表示する | Aの候補・手描き・履歴・確認状態を変えず、再表示後にだけ編集操作を再開できる |
| DI-057 | 空の名前付きPJを管理表で開く | 0件で編集操作を無効にし、画像読込・PJ名変更・削除は操作できる |
| DI-058 | 完了PJを管理表で表示 | 状態が完了となり、画像数・作成日時と行ごとのZIP・削除を保持する |
| DI-059 | 完了PJの作業を再開して管理表を表示 | 同じPJが作業中へ変わり、作成日時は不変。応答の更新時刻が増え、表にその日時を表示する |
| DI-060 | 作業中PJと完了PJを残してサーバー再起動 | 管理表の名前・状態・画像数・元フォルダー・作成日時・更新日時をそれぞれ保持する |
| DI-061 | PJ内の2フォルダーのうち片方のパスを変更してPJを開く | 不足したフォルダーだけを再指定対象にし、利用可能な画像の読込を妨げない |
| DI-062 | 別フォルダーの同名画像を含むPJを開き直す | 両画像を別IDで復元し、各モザイク・除外・確認状態・履歴を混ぜない |
| DI-063 | 設定の保存前に閉じる | 再表示時は保存前の値となり、PJの画像や編集内容は変わらない |
| DI-064 | 一覧検索を0件になる文字列にする | 0件の案内となり、検索を消すと画像が戻る。PJ画像データを消さない |
| DI-065 | モザイク範囲全体を除外して「モザイクあり」保存対象を見る | 実際の適用範囲が0の画像を対象に含めない |
| DI-066 | モザイク候補を全OFFにして「モザイクあり」保存対象を見る | 有効な手描きも残っていなければ対象から外れる。候補レコードがあるだけでは含めない |
| DI-067 | 保存中に対象プルダウンを見る | 無効となり、開始時の対象を途中で変更できない |
| DI-068 | 一括保存を一時停止して再開 | 開始時の対象を引き継ぎ、完了済みファイルを重複出力しない |
| DI-069 | コピー保存先が未設定の単一保存を開く | 保存先未設定を示して保存開始を無効にする。選択ダイアログは開かない |
| DI-070 | コピー保存先が未設定の一括保存を開く | 保存先未設定を示して保存開始を無効にする。上書き可能な画像なら上書きへの切替を許可する |

## 処理別のメモリ・応答

初回準備前と、同じ操作を5回繰り返した各終了後を比較する。終了直後と待機後を分け、RAM・専用VRAM・DOM数・イベントリスナー数・Worker数のうち該当するものを見る。

| ID | 手順 | 期待値 |
| --- | --- | --- |
| DI-071 | 4K画像20枚を順に表示して先頭へ戻る | 未選択画像のデコード済みデータが総画像数分蓄積せず、現在画像と制限付きキャッシュで保持する |
| DI-072 | 複数候補のある4K画像を順に表示して先頭へ戻る | 旧画像の候補Bitmapが積み重ならず、現在画像の候補は全件表示する。現在の1組がキャッシュ上限を超えても候補を消さない |
| DI-073 | 400画像の一覧を各フィルターへ切り替える | 表示範囲に応じたDOM数へ戻り、切替回数×400のノードを保持しない |
| DI-074 | 画像の多い2つのPJを切り替える | 閉じたPJの画像・候補・描画用バッファを回数分保持せず、現在PJへ旧画像を表示しない |
| DI-075 | モザイク表示をOFFにする | モザイクのプレビュー用Workerを終了し、OFF中にその生成を続けない。2枚表示右側の候補点滅は設定どおり維持する |
| DI-076 | 塗りつぶしの完了後に別画像へ移動 | 旧画像の計算が新画像へ反映されず、終了後のCPU高負荷や旧バッファの保持が続かない |
| DI-077 | 設定画面を閉じる | 開閉回数分の切り離された画面要素やイベントリスナーを保持しない |
| DI-078 | PJ管理を閉じる | 閉じた管理表の行が開閉回数分蓄積しない |
| DI-079 | 候補の枠小窓を閉じる | 小窓用イベントリスナーが蓄積せず、次の枠変更を重複送信しない |
| DI-080 | 4K画像の一括保存を完了する | 保存終了後に出力全件分の画像バッファを保持せず、完了回数に応じて待機時RAMが増え続けない |
| DI-081 | 確認用PJを削除する | 削除PJの画像・候補・履歴のブラウザー参照とDBの関連データが残り続けない |
| DI-082 | モザイク筆でドラッグを継続する | マウスを離す前からモザイクが追従し、操作時間に応じて追従遅延が増えない |
| DI-083 | 除外筆でドラッグを継続する | マウスを離す前から除外が追従し、モザイク側へ手描きを追加しない |
| DI-084 | 2枚表示でドラッグを継続する | 両画面の同一画像座標へ描画が反映され、片側だけ操作終了まで止まらない |

## クリア操作の対象と確認状態

| ID | 手順 | 期待値 |
| --- | --- | --- |
| DI-085 | A・Bを選択して一括モザイククリアの確認を開く | 対象2件と表示し、全画像を消すという文言を出さない |
| DI-086 | 全画像モザイククリアの確認を開く | 非表示を除く対象4件と表示し、焼き込み済み画素は復元しないことを案内する |
| DI-087 | 選択2件のクリア確認を取消 | A・Bの範囲・確認状態・表示状態が変わらず、削除要求を送信しない |
| DI-088 | 確認済みAの単一クリアを確定 | 直後の画面・画像一覧API・DBがすべてAを未確認とし、対象外の確認状態を保持する |
| DI-089 | A・Bの選択クリアを確定 | 直後の画面・画像一覧API・DBがすべてA・Bを未確認とし、E・Fの状態を保持する |
| DI-090 | A・Bの選択クリア直後に「確認済み」の一括保存を開く | 対象はCの1件。A・Bと非表示E・Gを含めず、画面・画像一覧API・DBで対象集合が一致する |
| DI-091 | クリア直後にPJを閉じて再開 | 確認状態が閉じる前と一致し、再開したときだけ確認済みから未確認へ変化しない |
| DI-092 | クリア後にUndo | 範囲と確認状態が履歴位置へ戻り、画面・画像一覧API・DBで一致する |
| DI-093 | クリアのUndo後にRedo | 対象範囲が消え、画面・画像一覧API・DBで対象が未確認となる |
| DI-094 | A～Hの初期状態からAだけをクリア | Aだけが範囲なし・未確認となる。B・E・Fの範囲、C・E・Gの確認済み、E～Hの非表示を保持する |
| DI-095 | A～Hの全画像クリアを確定 | 表示中A～Dだけが範囲なし・未確認となる。E～Hの候補・手描き・確認状態・非表示と画像ファイルの画素は変えない |
| DI-096 | 全画像クリア後に一括保存の対象を「モザイクあり」にする | 対象0件を表示し、保存開始を無効にする |
| DI-097 | 全画像クリア後に一括保存の対象を「確認済み」にする | 対象0件を表示し、保存開始を無効にする |
| DI-098 | 全画像クリア後に一括保存の対象を「画像一覧のすべて」にする | 非表示のE～Hを含めずA～Dの4件を対象とする |
| DI-099 | 無名の作業で、パス入力したPNGに手描きモザイクを追加して左右反転し、Undo後に単一上書き保存する | 500にならず保存できる。出力はUndo後の向きと手描き範囲を反映し、名前付きPJを作成しない |
| DI-100 | 無名の作業で、手描きモザイクと左右反転をしたPNGをUndoなしで単一上書き保存し、同じ条件を一括上書き保存でも行う | どちらも500にならず、各出力の反転状態と範囲が開始時の対象ごとに一致する |
| DI-101 | apply・exclude・forced候補を含む4K画像を3候補と20候補で用意し、透過PNGの手描きモザイク・除外・除外消しゴムを加える。単一画像保存・一括画像保存、単一モザイクPNG・除外PNG、PJのモザイクZIP・除外ZIPを出力中にRAMの増加を見る | モザイクと手描きに通常／強制除外・除外消しゴムを反映し、透過部分の白いRGB値を範囲にせず、単体とZIPの対応PNGで局所範囲が一致する。候補数に比例して処理中のRAMが増え続けない |
| DI-102 | NetworkでPJ作成・改名・完了・閉じる・開く・再開・フォルダー読込の応答を遅延または切断する。各要求の expectedProjectId、expectedCatalogGeneration、応答の catalogGeneration と直後の `/api/images` を確認する | 遷移要求は表示中PJと世代を期待値として送る。古い世代は stale_catalog として受理されず、応答喪失・エラー後は `/api/images` のPJ・画像・catalogGeneration を再取得して画面と一致させる |
| DI-103 | 2つのPJにブラウザーファイルとブラウザーフォルダーを多数保存してから、一方を削除し、もう一方を閉じて再開する。DevTools Applicationで projectSources の projectId index と削除完了を確認する | PJごとの復元・削除は projectId index を使い、削除要求の完了後に該当PJのブラウザー保持ソースだけが残らない。別PJの保持ソースと復元結果は変わらない |
| DI-104 | ブラウザー保持ソースを持つPJを削除する要求の応答をNetworkで切断し、直後にページを閉じて開き直す。別タブで同じPJの削除を同時に開始し、一方の要求を4xxで拒否させる場合も確認する | 削除済みまたはサーバーに存在しないPJの保持ソースは次回起動時に消える。応答喪失や起動時にPJが残る場合はcleanup intentと保持ソースを残し、明確な4xx拒否はその要求のintentだけを解除する。別タブのintentを消さない |
| DI-105 | 大規模なPJのモザイクZIPまたは除外ZIPを出力中に、別のPJで候補または手描き範囲を編集して保存する。出力中と完了後にタスクマネージャーでMozarieの保存応答と `workspaces.sqlite3-wal` の大きさを見る | 別PJの編集は待たされず保存され、WALが出力時間に比例して増え続けない。ZIP内の各PNGは出力開始時点のPJ状態で揃い、途中編集前後の世代を混在させない |
| DI-106 | 大規模なPJのZIPを開始後、まだZIPへ書き出されていない画像の候補または手描き範囲を更新して保存する。ZIP内の対応PNGと、保存後のPJ画面・DB状態を照合する | ZIP内の全PNGは開始時点の世代で揃い、更新した画像のZIPは更新前、PJ画面・DBは更新後の範囲になる |
| DI-107 | 大規模ZIPの開始から完了まで、`workspaces.sqlite3` と同じデータフォルダー内の `mozarie-export-*` と空き容量を観察する。正常完了と、保存済みマスク不正で失敗するZIPのそれぞれで終了後も確認する | 一時SQLiteの容量はデータフォルダーでのみ一時的に増え、ZIPと全件マスクを同時にメモリへ保持しない。成功・失敗のどちらでも終了後に `mozarie-export-*` を残さない |
| DI-108 | 大規模ZIPの途中でMozarieを強制終了し、再起動後に同じデータフォルダー内の `mozarie-export-*` と空き容量を確認する | 起動時の整理後に一時SQLiteを残さず、途中ZIPの状態を通常のPJデータや次の書出しへ混ぜない |
| DI-109 | Aの確認済み・非表示切替、Undo/Redo、候補一括操作を送信直後に別PJへ切り替える | PJ切替との競合では操作と切替が直列化される。操作が先なら旧PJだけを更新してから切り替え、切替が先なら旧要求を `stale_catalog` として拒否する。切替先PJのDBと画面を変更せず、500を返さない |
| DI-110 | A・Bへの候補一括操作を開始直後に別PJへ切り替える | PJ切替との競合では、A・Bの候補状態と履歴は両方変更されてから切り替わるか、両方保持して切り替わる。切替先PJに片方だけの確定を残さない |
| DI-111 | 完了PJ Aで候補・手描き・反転・保存操作を開始後、PJ Bを開くか、PJ Aを再開して同じIDで開き直す | 遅延したPJ A要求は `stale_catalog` となり、PJ A・PJ BのDBと画面を更新しない |
| DI-112 | Aにはapply候補、Bにはapply候補なしまたは元画像変更未確認の状態でA・Bへapply候補一括操作を送る | `candidate_not_found` または `source_mismatch` となり、Aの候補状態・候補revision・履歴も変更しない。DBまたは候補データ読込失敗時もA・Bとも公開前の候補状態と履歴を保持する |
| DI-113 | タブAで並列数を2以上にしたbrowser restore/importを実行中に、転送間でPJ A→B→Aへ切り替え、タブBから同じPJの古い一覧で変更または復元を送る | 同時転送中の同一import sessionだけは開始時の世代で並列追加でき、各公開で世代を更新する。active 0後の次転送は直前公開世代と一致しなければ拒否する。タブBの古い要求と別sessionの追加は拒否され、追加済み画像を上書きしない |
| DI-114 | Aを保存して元画像を削除した直後、削除前の一覧を持つ別タブからAへの候補・手描き・反転変更を送る | 保存成功または同token再試行応答で世代が更新され、遅延要求は `stale_catalog` となる。Aを再表示せず、DBに候補・手描き・反転を追加しない |
| DI-115 | browser restore/importを正常完了した直後に続けて別のimportを開始する | 前の転送の成功応答が返る前にactive数を解放し、完了要求が保留されず次のimportを開始できる |
| DI-116 | 並列browser restore/importの途中で1件を失敗または取消し、その直後に別のimportを開始する | 最後の転送終了で取消状態を含む完了要求を解放する。失敗・取消した非稼働sessionは次の明示開始で置換でき、別sessionの画像を変更しない |
| DI-117 | browser restore/importの各転送終了後、完了要求の前に別PJへ切り替えてから元のsessionを完了する | 開始時のPJ・session IDが一致すると安全に解放する。切替先のPJや一覧を変更せず、完了済み・未知sessionの終了は冪等で、別session IDでは追加を受け付けない |
| DI-118 | native PJ Aからbrowser-only PJ B、空PJ作成、閉じる、開いているPJの削除を含め、タブAでフォルダー読込、PJ作成・名前保存・改名・完了・再開、変更元再指定、browser source追加の各応答をNetworkで遅延し、応答直前にタブBで別のPJ遷移を試す | タブAの成功応答と `/api/images` に含まれるPJ・画像・root・sources・needsSource・catalogGenerationは同じ遷移時点の組合せとなる。native sourceのないPJまたは空作業で旧PJのrootを返さない。タブBは応答スナップショット取得後に実行されるか、古い世代として拒否され、異なるPJの内容と世代を混ぜない |
| DI-119 | PJ Aの複数画像へ確認済み・非表示の一括変更を送信し、応答前に別タブでPJ Bへ遷移する | 一括変更はPJ AのDBと画面へ全件そろって反映されてから切り替わるか、古い要求として拒否される。PJ Bの状態やcatalogGenerationをPJ Aの応答に混ぜない |
| DI-120 | 複数の元フォルダーを持つPJを開く際、後半の元フォルダーまたは保存済み候補を読めない状態にして失敗させる。直前に開いていた別PJと、開こうとしたPJを再確認する | 画面は直前のPJ・画像・候補・catalogGenerationを保持する。開けなかったPJは元フォルダーの記録、確認済み状態、反転状態、PJ状態を変更しない |
| DI-121 | 保存済み候補のメタデータまたはマスクが不正なPJフォルダーを通常のパス入力で読み込む。失敗応答後に現在の一覧とNetworkのcatalogGenerationを確認する | 読込失敗前のPJまたは無名一覧、候補、catalogGenerationを保持する。失敗した読込先を途中状態で公開せず、古い一覧を保持したブラウザーと食い違わない |
| DI-122 | 4K画像を含む多数画像で候補の有効・無効、削除、枠pxを一括変更する。処理中に別画像の一覧取得と画面更新を繰り返し、完了直後にPJを切り替える | 対象画像の候補・有効モザイク・履歴は全件まとめて確定するか、全件保持する。処理中も一覧取得と既存画面の更新が長時間停止せず、切替後のPJへ候補や履歴を混ぜない |
| DI-123 | タブAの候補・手描き・反転・確認済み・一括操作・保存・検出をNetworkで遅延し、タブBでPJを切り替える。タブAの409応答後、同名PJなどの確定400応答後にも一覧・現在画像・候補を確認する | stale_catalogの409後は一度だけ権威スナップショットへ置換し、PJ・root・画像・source一覧・不足native source表示・現在画像・候補キャッシュを混ぜない。確定400では同一PJの下書きとhandle対応を不要に初期化しない |
| DI-124 | ブラウザー画像のimport、単一保存、一括保存、検出完了、マスククリア、PJ Undo/RedoをそれぞれNetworkで遅延し、`/api/images` の要求送信後・応答前に別タブでPJを切り替える。置換後に元画像を外部変更して保存も試す | 完了側は一覧取得の要求直前に保持したPJ・catalogGenerationと応答を照合する。自身の更新だけなら編集状態を保持し、別PJ遷移なら一覧全体をPJ・画像・root・source一覧・needsSourceを含む権威スナップショットへ置換する。復元したfile/directory handleも元画像のサイズ・更新時刻を保持し、外部変更を保存前に拒否する。旧PJの現在画像、候補、root、cache、source handleを新PJの画像へ残さない |
| DI-125 | PJのブラウザー画像を単体・複数選択で削除し、単体・一括コピー保存の「コピー後に元画像を削除」でも成功応答を切断してから再起動する。削除中に別タブで起動時照合を行い、削除拒否の場合も確認する | 削除commit前に記録した片付け対象は、再起動後の権威PJ状態で削除済みなら該当画像の保存済みハンドルだけを消す。存在中・未確定時は記録を保持し、明確な拒否を受けた削除要求だけ記録を消す |

## 絞り込み・選択・削除後の現在画像

この節は左一覧と一覧表示の両方で実施する。一覧表示は検索を空、フォルダーを全体にしてから始める。M=モザイクあり、U=モザイク無し、R=確認済み、N=未確認、H=非表示である。条件なしではA～Hをすべて表示する。一つ以上の条件を選んだ場合は和集合で、Hを選ぶと非表示のE～Hを表示し、Hを選ばない場合は非表示画像を表示しない。

| ID | 選択した条件 | 左一覧／一覧表示の期待集合 |
| --- | --- | --- |
| DI-126 | なし | A・B・C・D・E・F・G・H |
| DI-127 | M | A・B |
| DI-128 | U | C・D |
| DI-129 | R | A・C |
| DI-130 | N | B・D |
| DI-131 | H | E・F・G・H |
| DI-132 | M・U | A・B・C・D |
| DI-133 | M・R | A・B・C |
| DI-134 | M・N | A・B・D |
| DI-135 | M・H | A・B・E・F・G・H |
| DI-136 | U・R | A・C・D |
| DI-137 | U・N | B・C・D |
| DI-138 | U・H | C・D・E・F・G・H |
| DI-139 | R・N | A・B・C・D |
| DI-140 | R・H | A・C・E・F・G・H |
| DI-141 | N・H | B・D・E・F・G・H |
| DI-142 | M・U・R | A・B・C・D |
| DI-143 | M・U・N | A・B・C・D |
| DI-144 | M・U・H | A・B・C・D・E・F・G・H |
| DI-145 | M・R・N | A・B・C・D |
| DI-146 | M・R・H | A・B・C・E・F・G・H |
| DI-147 | M・N・H | A・B・D・E・F・G・H |
| DI-148 | U・R・N | A・B・C・D |
| DI-149 | U・R・H | A・C・D・E・F・G・H |
| DI-150 | U・N・H | B・C・D・E・F・G・H |
| DI-151 | R・N・H | A・B・C・D・E・F・G・H |
| DI-152 | M・U・R・N | A・B・C・D |
| DI-153 | M・U・R・H | A・B・C・D・E・F・G・H |
| DI-154 | M・U・N・H | A・B・C・D・E・F・G・H |
| DI-155 | M・R・N・H | A・B・C・D・E・F・G・H |
| DI-156 | U・R・N・H | A・B・C・D・E・F・G・H |
| DI-157 | M・U・R・N・H | A・B・C・D・E・F・G・H |

以下はPJなしと名前付き・作業中PJの両方で実施する。確認を取り消した場合は削除要求を送らず、状態・現在画像・キャンバス・選択集合を変えない。

| ID | 手順 | 期待値 |
| --- | --- | --- |
| DI-158 | 左一覧の表示集合を3件にし、先頭・中間・末尾をそれぞれ現在画像として「削除」を確定する | 操作前の左一覧表示順で、先頭と中間は次の一致画像、末尾は直前の一致画像を開く。削除画像の白い未選択表示や古いキャンバスを残さない |
| DI-159 | 左一覧の表示集合を1件にして、その画像を「削除」で削除する。表示集合の全件を順に削除する場合も確認する | 残存候補がないときだけ未選択・空キャンバスとなる。削除済み画像を現在画像として残さない |
| DI-160 | 現在画像を左一覧の絞り込み外にしたまま「削除」を確定する | 操作前の左一覧表示集合の先頭一致画像を開く。絞り込み外の次画像や全画像順の次画像を開かない |
| DI-161 | 表示集合の先頭・中間・末尾・1件・絞り込み外の可視な現在画像で「確認済みにして次へ」と「非表示にして次へ」をそれぞれ実行する | 両操作とも押下時の左一覧表示順だけを対象にする。次の一致画像だけを開き、末尾と1件では確認済みまたは非表示になった現在画像とキャンバスを保持し、直前へ戻らない。絞り込み外では先頭の一致画像を開く。Hで表示する既に非表示の現在画像は閲覧できるが、処理対象にしない。対象外画像を開かない |
| DI-162 | 一覧表示で絞り込み、検索、フォルダーを変えながら一括選択する。選択後に条件を変え、「一括操作」を開く | 表示外になった画像は選択数と実際の対象から外れる。表示中・選択中と表示する一括操作は、その時点の一覧表示集合内だけを対象にする。PJ全体・すべてと明示した保存操作の対象は変えない |
| DI-163 | 一覧表示の表示集合で、現在画像を含まない複数画像を一括削除する | 現在画像とキャンバスを保持する。削除対象、選択数、一覧表示、残る画像を一致させる |
| DI-164 | 一覧表示の表示集合で、現在画像を含む複数画像を一括削除する。現在画像が先頭・中間・末尾となる各場合を行う | 操作前の一覧表示順で削除されない次の一致画像、末尾では削除されない直前一致画像を開く。削除対象と現在画像を混ぜず、古いキャンバスを残さない |
| DI-165 | 一覧表示の現在画像を含む選択で、表示集合の全件を一括削除する | 残存候補がないときだけ未選択・空キャンバスとなる。選択数は0となり、削除済みIDを操作対象にしない |
| DI-166 | DI-158～165の単体・一括削除で確認を取消す | 削除要求を送らず、一覧順・絞り込み・選択集合・現在画像・キャンバス・PJデータを保持する |
| DI-167 | DI-158～165の単体・一括削除でサーバーから409を返す | 権威一覧へ同期し、成功表示や削除後の選択を先行して出さない。現在画像とキャンバスは同期後の一覧にだけ対応する |
| DI-168 | DI-158～165の単体・一括削除で応答を切断し、再接続後に一覧とPJを開き直す。現在以外の単体削除も行う | currentまたはpendingの削除が確定した場合だけ、操作前の左一覧または一覧表示順で次の残存候補、末尾では直前候補、候補なしでは未選択へ整合する。現在以外の単体削除では削除確定後も操作前のpending画像、なければ現在画像を権威一覧から選び直す。未確定も同じ選択を復元し、古いキャンバスや白い未選択状態を残さない |
| DI-169 | 左一覧の表示集合を3件、1件、0件にし、現在画像が表示内と表示外の各状態で、前へ・次へボタン、previous・next、previousVisible・nextVisible、Home、Endをそれぞれ実行する | 各操作、位置表示、ボタンの有効状態はすべて左一覧の表示集合だけを基準にする。表示外の現在画像からnext／nextVisibleは先頭一致、previous／previousVisibleは末尾一致へ進み、表示集合が0件では移動しない |
| DI-170 | 非表示画像を含むA～Hで「全画像を自動検出」を開く | A～Dの4件を対象として表示する。左一覧または一覧表示の絞り込みでは狭めず、非表示だけを常に除外する |
| DI-171 | 一覧表示で絞り込み・検索・フォルダーを組み合わせて表示集合を作り、選択後に条件を変えて「一覧から削除」を実行する。確認の取消、409、応答切断も個別に行う | 選択数、確認件数、送信する画像ID、削除対象は実行時の表示集合との積集合だけになる。取消・409・切断では権威応答前に削除や現在画像移動を確定しない |
| DI-172 | 一覧表示で絞り込み・検索・フォルダーを組み合わせて表示集合を作り、選択後に条件を変えて「非表示」を実行する。要求の取消、409、応答切断も個別に行う | 選択数と送信画像IDは実行時の表示集合との積集合だけになる。取消・409・切断では対象外の非表示状態を変えず、確定応答後だけ一覧を更新する |
| DI-173 | 一覧表示で絞り込み・検索・フォルダーを組み合わせて表示集合を作り、選択後に条件を変えて「再表示」を実行する。要求の取消、409、応答切断も個別に行う | 選択数と送信画像IDは実行時の表示集合との積集合だけになる。取消・409・切断では対象外の表示状態を変えず、確定応答後だけ一覧を更新する |
| DI-174 | 一覧表示で絞り込み・検索・フォルダーを組み合わせて表示集合を作り、選択後に条件を変えて「モザイククリア」を実行する。確認の取消、409、応答切断も個別に行う | 選択数、確認件数、送信画像ID、クリア対象は実行時の表示集合との積集合だけになる。取消・409・切断では候補・手描き・確認状態を先行変更しない |
| DI-175 | 一覧表示で絞り込み・検索・フォルダーを組み合わせて表示集合を作り、選択後に条件を変えて「自動検出」を実行する。ダイアログ取消、409、応答切断も個別に行う | 表示した対象件数と検出要求の画像IDは実行時の表示集合との積集合だけになる。取消・409・切断では検出ジョブや候補を先行して開始・更新しない |
| DI-176 | 一覧表示で絞り込み・検索・フォルダーを組み合わせて表示集合を作り、選択後に条件を変えて「確認済」を実行する。要求の取消、409、応答切断も個別に行う | 選択数と送信画像IDは実行時の表示集合との積集合だけになる。取消・409・切断では対象外を確認済みにせず、確定応答後だけ状態を更新する |
| DI-177 | 一覧表示で絞り込み・検索・フォルダーを組み合わせて表示集合を作り、選択後に条件を変えて「未確認」を実行する。要求の取消、409、応答切断も個別に行う | 選択数と送信画像IDは実行時の表示集合との積集合だけになる。取消・409・切断では対象外を未確認にせず、確定応答後だけ状態を更新する |
| DI-178 | 左一覧と一覧表示に絞り込み・検索・フォルダーを設定してから「全画像を自動検出」を実行する。ダイアログ取消、409、応答切断も行う | 明示的な全画像操作は表示集合と無関係に非表示を除くPJ全体の画像IDを対象とする。取消・409・切断ではジョブと候補を先行変更しない |
| DI-179 | 左一覧と一覧表示に絞り込み・検索・フォルダーを設定してから「全画像モザイククリア」と「一覧クリア」をそれぞれ実行する。確認取消、409、応答切断も行う | 明示的な全体操作は表示集合を使わず、非表示を除く画像だけと確認件数・送信画像IDが一致する。取消・409・切断では一覧・候補・手描き・現在画像を先行変更しない |
| DI-180 | 左一覧と一覧表示に絞り込み・検索・フォルダーを設定して、「画像一覧のすべて」を対象に一括保存を開く。開始前の取消、409、応答切断も行う | 保存対象の表示件数と送信画像IDは非表示を除くPJ全体と一致し、絞り込み・検索・フォルダーで狭めない。取消・409・切断では出力・確認状態・編集データを先行変更しない |
| DI-181 | 「削除」は表示集合の先頭・中間・末尾・1件・絞り込み外の現在画像で確認を取消し、続けて409と応答切断を行う。「非表示にして次へ」「確認済みにして次へ」は同じ各位置の可視な現在画像でNetworkの応答を遅延させ、409と通信断を個別に行う | 3操作とも操作前の表示集合を使う。削除だけは末尾で直前、候補なしで未選択となる。非表示にして次へと確認済みにして次へは次だけを開き、末尾と1件で次がなければ変更後の現在画像とキャンバスを保持し、直前へ戻らない。絞り込み外の現在画像では3操作とも先頭の一致画像を開く。Hで表示する既に非表示の現在画像は閲覧できるが、非表示処理の対象にしない。削除の取消、各操作の遅延中・409・通信断では応答確定前に現在画像とキャンバスを移動・消去しない |
| DI-182 | 表示集合が1件、または現在画像の後ろに一致候補がない状態で「非表示にして次へ」を確定する。Hを選んで非表示後に現在画像が表示集合に残る場合も行う | 非表示成功後に次の一致画像がなければ、非表示になった現在画像とキャンバスを保持し、未選択・空キャンバスにも直前画像にも移らない。Hで現在画像が表示集合に残る場合も同じ結果とする |
| DI-183 | 一覧表示で現在画像とは別の画像への選択読込をNetworkで遅延し、遅延中の画像を含める一括削除と、含めない一括削除を行う。現在画像も同時に削除する場合、409と応答切断も行う | 削除開始時に遅延中選択を明示的に失効し、削除対象を現在画像・候補・キャンバスとして再表示しない。削除確定後は遅延中の削除対象を優先した操作前の一覧表示順で残存候補を選び、遅延中画像が削除対象外または未確定なら削除前のpending画像、なければ現在画像を権威一覧から選び直す。候補がなければ未選択となる |
| DI-184 | 現在画像に未保存の手描きモザイクまたは除外を残し、現在画像以外の単体削除を成功・409・応答切断で行う | 現在画像と編集状態が権威一覧で有効なままなら、強制再読込せず未保存の手描き・キャンバス・下書きを保持する。再同期で現在画像が無効になった場合だけ、権威一覧に残るpending画像、なければ現在画像を選び直す |
| DI-185 | 左一覧と一覧表示でM・U・R・N・Hの組合せ、検索、フォルダーを繰り返し切り替え、各表示集合を先頭から末尾まで確認する。途中で仮想スクロールして再度切り替える | 論理集合だけでなくカードと行の視覚順・DOM順も元のA～H順を保つ。条件を外して再表示したカード、別行へ移った行、仮想表示へ戻った行の順序が過去の表示順に残らず、Tab順・読み上げ順・画面上の順序と一括選択・削除後の次画像の順序が一致する |
| DI-186 | DevToolsのNetworkで、現行画面から画像とPJを各1回ずつ削除確定する。各DELETEのヘッダーと本文を確認する | ブラウザーから送る画像・PJのDELETEには本文と`Content-Length`がなく、`X-Mozarie-Expected-Project-Id`と`X-Mozarie-Expected-Catalog-Generation`を送る。対象だけを削除し、残る画像・PJ・候補・手描き編集を変えない。候補削除は画面では下書きのPOST保存であり、このDELETE確認には含めない |
| DI-187 | 確認用のローカルHTTPクライアントで、画像・PJ・候補・手描き作業領域の4 DELETEルートへ旧形式の`application/json`本文（期待PJ ID・カタログ世代を含むJSONオブジェクト）を送る | 本文を全て読んでから同じ世代照合を行い、対応する本文なしDELETEと同じ結果になる。JSON以外、壊れたJSON、JSON配列、正しくない世代は削除せず、該当する400または409を返す |
| DI-188 | 同じクライアントで、DELETEに`Transfer-Encoding`、複数または非ASCII・非数値の`Content-Length`、上限超過、4301桁以上のASCII数字だけの`Content-Length`を送る。GETへ0以外の`Content-Length`と本文を送る | いずれも400と`Connection: close`を返し、接続を再利用できない。後続要求を処理せず、GETは状態を変えない |
| DI-189 | 通常JSON POSTへ、`Transfer-Encoding`、`Content-Length`なし・0・不正・上限超過、壊れたJSON、JSON配列を送る。バイナリ画像追加へ本文付きで不正なメタ情報または古い期待PJ ID・世代を送る | 未読本文を残す失敗は400または409と`Connection: close`を返し、次の要求を同じ接続で処理しない。画像追加はファイル・画像一覧・PJ・世代を変更しない |
| DI-190 | 作業領域を再作成する必要がある状態でDELETEを送り、同じ接続で別の要求を続ける | DELETEの応答は`Connection: close`を返し、後続要求を処理しない。新しい接続からの復旧操作だけを受け付ける |
| DI-191 | keep-aliveを維持する確認用ローカルHTTPクライアントで、現在表示中の画像を本文なしDELETEで削除し、同じ接続で`/api/images`、次に表示した画像の`/api/candidates/{imageId}`を取得する。画面でも次画像へ移ったことを確認する | DELETEと続く2つのGETが成功し、接続を閉じない。削除画像は一覧・候補・編集対象から消え、次画像だけを表示する。候補取得は次画像の候補だけを返し、削除画像または古い本文を次の要求として扱わない |
| DI-192 | keep-aliveを維持する確認用ローカルHTTPクライアントで、PJを本文なしDELETEで削除し、同じ接続で`/api/projects`と`/api/images`を取得する。画面もPJ一覧と作業画面を更新する | DELETEと続くGETが成功し、削除PJはPJ一覧と現在カタログから消える。残るPJ・画像と世代はサーバーの応答どおりで、削除要求のヘッダーや本文が次のGETに混ざらない |
| DI-193 | 古い期待PJ IDまたはカタログ世代で本文なしDELETEを送り409にし、同じ接続で`/api/images`を取得する | DELETEは状態を変更せず409を返すが接続は維持する。直後のGETは現在の権威スナップショットと世代を返し、画面はその一覧へ再同期する |
| DI-194 | 確認用のローカルHTTPクライアントで、候補を1件、手描き作業領域を1件用意し、それぞれの直接APIルートへ本文なしDELETEを送る。各DELETEの直後に同じ接続で候補一覧または手描き作業領域GETを送る | 候補と手描き作業領域のDELETEも期待値ヘッダーだけで成功する。直後のGETは削除済み状態を返し、別画像・別PJの候補と手描き編集を変更しない |
| DI-195 | 有効な`Content-Length`とJSON本文を持つDELETEへ、誤った`X-Mozarie-Token`、次に誤った`Origin`を付ける。同じ接続で続くGETも送る | 各DELETEは削除せず403と`Connection: close`を返す。後続GETは同じ接続で処理されず、新しい正しい接続からは元の画像・PJ・候補・手描き編集を取得できる |
| DI-196 | DI-186〜195の全操作後にMozarieのサーバーログを確認する | `501`、`Unsupported method`、またはDELETE本文が後続要求として解析された記録がない。各応答は手順で示した成功・400・403・409と接続状態に一致する |
| DI-197 | 確認用のローカルHTTPクライアントで、有効なバイナリ画像追加のヘッダーと本文に、4301桁以上のASCII数字だけの`X-Mozarie-Expected-Catalog-Generation`を付ける | 本文を読まずに400と`Connection: close`を返し、同じ接続の後続要求を処理しない。画像・一覧・PJ・カタログ世代・インポート状態を変更せず、サーバーログに500・501・`Unsupported method`がない |
| DI-198 | 確認用のローカルHTTPクライアントで、有効なバイナリ画像追加のヘッダーと本文に、4301桁以上のASCII数字だけの`X-Mozarie-File-Mtime`を付ける | 本文を読まずに400と`Connection: close`を返し、同じ接続の後続要求を処理しない。画像・一覧・PJ・カタログ世代・インポート状態を変更せず、サーバーログに500・501・`Unsupported method`がない |
| DI-199 | 確認用のローカルHTTPクライアントで、有効なバイナリ画像追加のヘッダーと本文に、4301桁以上のASCII数字だけの`X-Mozarie-File-Size`を付ける | 本文を読まずに400と`Connection: close`を返し、同じ接続の後続要求を処理しない。画像・一覧・PJ・カタログ世代・インポート状態を変更せず、サーバーログに500・501・`Unsupported method`がない |
| DI-200 | keep-aliveを維持する確認用のローカルHTTPクライアントで、認証・期待値ヘッダーを正しく付けた通常JSON POSTのJSONオブジェクト内に4301桁以上の整数リテラルを入れ、同じ接続で`/api/images`を続けて送る | POSTは400と`input_invalid`を返すが接続を閉じない。続くGETは200で現在の権威一覧と世代を返し、画像・PJ・カタログ世代を変更しない。サーバーログに500・501・`Unsupported method`がない |
| DI-201 | 確認用の使い捨てMozarie環境で、画像追加用の`session_base_dir`を書込み不能な既存ファイルへ向ける。本文付きの有効な`/api/import/file`を送った後、同じ接続でGETを続けて送る | セッション用ディレクトリ作成失敗は500と`Connection: close`を返し、続くGETを同じ接続で処理しない。画像・一覧・PJ・カタログ世代・インポート状態を変更せず、サーバーログに501・`Unsupported method`がない |
| DI-202 | 確認用の使い捨てMozarie環境で、画像追加の一時保存先に本文より小さい容量制限を設定する。本文付きの有効な`/api/import/file`を送り、書込みまたはflush失敗後に同じ接続でGETを続けて送る | 本文の途中の保存失敗は500と`Connection: close`を返し、部分的な一時ファイルを残さず、続くGETを同じ接続で処理しない。画像・一覧・PJ・カタログ世代・インポート状態を変更せず、サーバーログに501・`Unsupported method`がない |
| DI-203 | Eを非表示にしたまま、全・現在・選択検出、境界検出、候補変更・削除、手描き・反転、現在・全・選択クリア、単体保存と単体マスク出力を画面と直接APIで試す | UIは開始できず、直接APIは`image_hidden`で拒否する。Eの候補・手描き・反転・確認状態を変えず、A～Dの閲覧・確認済み管理・Eの再表示と完全削除は使える |
| DI-204 | 検出または保存の開始直後に対象Aを非表示へ変更し、別画像Eを非表示から再表示する | 処理対象Aの非表示変更は`operation_in_progress`で拒否し、処理対象外Eの再表示は許可する。処理完了後にAを非表示にできる |
| DI-205 | 非表示Eを含むPJで、PJ行のモザイクZIP・除外ZIPを開始し、開始直後にAを非表示へ変更する | ZIPは開始時点で可視のA～Dだけを含み、Eの候補・手描きBLOBを読まない。開始後のAの非表示変更はそのZIPの内容を変えず、次回ZIPからAを除外する |
| DI-206 | Aへ未保存の手描きを残して、単体非表示・コンテキストメニューの非表示・非表示にして次へを各々実行し、Aを再表示する | 非表示要求より先に下書きを保存し、再表示後のAは直前の手描き・候補・確認状態を保持する。保存失敗時は非表示にせず下書きを残す |
| DI-207 | A・Bを同じPJ履歴グループへ記録し、Bを非表示にしてAからUndo/Redoを画面と直接APIで試す | 非表示を含むグループ全体を原子的に復元する。A・Bの候補・手描き・履歴カーソル・確認済み・非表示状態が履歴位置へ揃い、対象外を変更しない |
| DI-208 | 単体ブラウザ保存のレンダー中、発行済み保存トークンのコミット前、期限切れトークン後に、それぞれ対象Aを非表示にする | レンダー中と有効トークン中は`operation_in_progress`で拒否し、保存完了または期限切れ後は非表示にできる。再表示は常に許可し、レンダー済みデータへ非表示画像を混ぜない |
| DI-209 | 非表示Eだけを一覧選択してから可視Aを加え、Eを現在画像にして一括操作、Undo/Redo、候補の検出範囲・適用範囲表示を確認する | 選択変更直後から、Eだけなら自動検出・モザイククリアを無効、Aを加えると有効にする。EのUndo/Redoは履歴に従って使え、非表示・確認済みを含む状態を復元する。候補の検出範囲・適用範囲表示、再表示、確認済み管理、完全削除も使える |
| DI-210 | 現在・選択・全画像の各自動検出について、開始直前と正常完了直後の画像一覧を取得し、件数・順序・全画像ID・相対パス・カタログ世代を比較する。候補が0件となる`004.png`も対象へ含める | どの検出経路でも一覧の件数・順序・全画像ID・相対パス・カタログ世代が完全に一致する。`004.png`を候補0件でも一覧に残し、候補の置換や候補リビジョン更新を画像の削除として扱わない |
| DI-211 | 現在・選択・全画像の各自動検出を取消・一時停止後に取消・モデル失敗・通信切断で終了し、開始前後の画像一覧と元画像ファイルの存在・サイズ・更新日時を比較する | 成否や候補数にかかわらず画像一覧の件数・全画像ID・相対パスと元画像ファイルを変更しない。取消・失敗時は対象全件の候補、確認済み、履歴を開始前のまま保持し、対象全件が成功した時だけ候補を一括公開する |
| DI-212 | SQLiteのバインド変数上限が異なる環境で、上限を越える画像IDの状態取得、削除、候補復元、手描き状態取得、import失敗時のロールバックを行う | 接続が報告する実際の変数上限から操作ごとに分割し、全対象を処理する。途中で一部だけ確定せず、固定件数による失敗を出さない |
| DI-213 | 別プロセスで作業DBをロックしたままMozarieのPJ操作を開始し、その後ロックを解放する。別ケースではMozarieを終了する | ロック解放後は同じ操作を続行する。終了時は待機中の操作が終了へ移り、固定秒数のタイムアウトでPJ状態を中途半端にしない |
| DI-214 | browser importの`X-Mozarie-Source-Id`、`X-Mozarie-Import-Session`、完了要求のsessionIdへ、正規UUID・大文字UUID・任意文字列をそれぞれ送る | 正規の小文字UUIDだけを受け入れる。不正値は画像・PJ・カタログ世代・他sessionを変更せず入力エラーになる |
| DI-215 | 長い候補・マスク処理で状態ロックが使われている間に、`/api/job`を繰り返し取得する | 取得は待機せず、最後に公開した同一時点の進捗を返す。処理が進むと次の取得で新しい進捗へ更新し、部分的なJob状態や500を返さない |
| DI-216 | PJを開く際に元画像変更の照合または再開状態の永続化後、一覧公開・候補復元・キャッシュ整理を失敗させる | 開く前のPJ表示とDBの完了/作業中状態、元画像照合により変化した反転版を一致させて復元する。失敗したPJの一部状態を公開せず、次回の開き直しを妨げない |
| DI-217 | 現在画像で赤い「削除」ボタンと既定Deleteショートカットをそれぞれ実行し、確認を取消してから再実行する | 毎回確認を表示する。確認文は元画像・候補・手描き・履歴・確認/非表示を完全削除し、戻せず、同じ元画像を使う他PJで不足し得ることを明記する。取消時は元画像と作業状態を一切変更しない。 |
| DI-218 | ネイティブフォルダーの単体・表示中選択一括削除を実行し、各元画像とSQLiteの画像・候補・手描き・履歴・確認/非表示を確認する | 成功画像だけ元画像とMozarie内データを完全削除し、Undo/Redoでは復元できない。単体・一括とも赤い操作である。非表示、一覧クリア、PJ削除、候補の×削除は元画像を残す。 |
| DI-219 | ネイティブ元画像を削除直前に外部変更・消去し、複数選択では有効画像と無効画像を混在させる | 更新日時・サイズを再照合する。失敗画像は一覧・元画像・作業状態を保持し、有効画像だけ削除する。DB削除失敗を起こした場合は一時renameした元パスを復元する。 |
| DI-220 | ブラウザーのフォルダー読込で、書込権限あり・拒否・親directory handleなし・元画像外部変更の各状態で単体・一括削除する | 削除確認直後、下書き保存や通信の前に親directory handleの`readwrite`権限を要求する。`parentHandle.removeEntry`直前に親からファイルhandleを再解決し、同一entry・サイズ・更新日時を照合する。条件を満たさない画像は一覧とMozarie内データを保持して理由を表示し、成功画像だけ完全削除する。 |
| DI-221 | ブラウザー元画像の完全削除で、サーバー応答だけを切断して同じ削除トークンの確定要求を再送する | 同じトークンは削除を二重実行せず、確定済みの画像一覧・成功件数・画像ごとの失敗理由を返す。終端結果の受領後にackし、元画像を先に消したブラウザーでも、応答消失後の再送で作業一覧を整合させる。 |
| DI-222 | 絞り込み表示の先頭・中間・末尾・1件で元画像を削除し、現在画像を含む一括削除も行う | 押下時の絞り込み表示順で次の残存画像を開き、末尾では前の残存画像を開く。表示対象が尽きたときだけ未選択にする。全画像順や絞り込み外画像を開かない。 |
| DI-223 | 自動候補・手描き候補のカードを表示する | 上段は候補名と右上の赤い×、下段はON/OFFや表示操作を置く。×は候補だけを削除し、元画像や他候補を削除しない。 |
| DI-224 | ブラウザー元画像の削除で、`removeEntry`後の確定前にタブを閉じ、Mozarieを再起動して同じPJを開く | 永続化した削除トークンの状態を再照会する。すでに消えたブラウザー元画像は同じトークンでMozarie内削除を一度だけ完了し、一覧・候補・手描き・履歴を整合させる。終端結果を受領したらackしてトークンを削除する。 |
| DI-225 | ネイティブ元画像のrename後にunlink不能を起こし、Mozarieを再起動する | 画像のMozarie内削除は成功として表示し、隔離ファイルの後処理件数を別表示する。DB削除済み画像を復元表示せず、隔離ファイルの後処理を起動時に再試行する。保留中はCMD warningとstatusの`cleanup_pending`で確認する。 |
| DI-226 | 元画像削除のprepare・status・cancel・ackを同一トークン、別トークン、不正UUIDで直接API確認する | UUID以外は拒否する。prepareはworkspace・PJ・カタログ世代・対象ID・元画像fingerprintを保存し、statusは再起動後も同じ結果を返す。cancelは未確定操作だけを取り消し、ackはcommitted/cancelledだけを削除する。 |
| DI-227 | ネイティブ元画像のrename直前、rename直後、SQLite確定直後にMozarieを停止して再起動する | rename前の隔離予定と進捗を永続化する。SQLite確定前は元パスへ復元してpreparedへ戻り、確定後は元画像を復元せず、画面キャッシュ整理とunlink後処理だけを再試行する。native-onlyのpreparedは黙ってcancelせず、未完了として再試行できる。 |
| DI-228 | prepare後に一覧世代を変え、複数画像のうち一部だけ元画像のパス・サイズ・更新日時・source kindを変えて確定する | prepare時の世代と対象fingerprintを画像ごとに現在レコードと再照合する。再照合できた画像だけ削除し、再照合できない画像はstale理由を表示して元画像・Mozarie内データを保持する。 |
| DI-229 | ブラウザー元画像の`removeEntry`直前にタブを閉じ、再起動後に保存済み親・ファイルhandleで存在確認する | `deleting`を先にIndexedDBへ保存する。親に同名entryがなければbrowserDeletedとして確定し、存在する場合は削除を取消または再試行する。権限不明・NotFound以外の例外はpendingを保持して再照会でき、未確認の元画像をMozarie側だけ削除しない。 |
| DI-230 | ブラウザーまたはネイティブの一括削除で、全件preflight失敗、prepare時一部失敗、削除時一部失敗をそれぞれ起こす。prepare失敗IDを直接確定要求にも混ぜる | 成功・失敗の件数、相対パスまたは画像ID、理由を画面へ表示する。ブラウザー内だけのpreflight失敗はブラウザーconsole、サーバーへ到達したprepare・確定・後処理の失敗はCMDログにも出す。同じ画像は最初の理由だけを1件返す。prepareされなかった操作はIndexedDBのトークンを残さず、prepare済みで削除対象が0件ならcancel→status→ackしてトークンを削除する。 |
| DI-231 | 元画像削除の確認でキャンセル、ブラウザー親directoryの`readwrite`許可、拒否を順に行う | `requestPermission`は確認ダイアログの実行クリック内で開始し、close後や下書き保存後に開始しない。キャンセル時は権限要求を出さず、複数親の拒否・例外は画像別の失敗として集計する。 |
| DI-232 | IndexedDBのopen、transaction、putをそれぞれ失敗させてブラウザー元画像削除を実行する | 復旧intentを保存できないことを明示して`removeEntry`へ進まない。prepare前にローカルtokenを保存し、prepare応答消失時は次回起動でstatus照会する。同じ画像の新しいprepareは古いprepared receiptを置換し、終端receiptはackで削除する。 |
| DI-233 | rename後の元パスに外部ファイルを作成し、隔離ファイルも外部変更、共有、権限拒否、I/O到達不能にした状態でDB失敗または再起動復旧を行う | 元パスと隔離ファイルが同時にある場合、または隔離ファイルの親・名前・fingerprintが記録と違う場合、どちらも上書き・削除しない。隔離ファイルが実際に存在しない場合だけ後処理済みと扱い、他のOSErrorは安全な例外種別を理由としてpending保持する。pendingも起動ごとに再検証し、一時的な共有・権限・I/O失敗が解消した場合は安全にunlinkしてcommittedへ進む。`restore_conflict`として相対パスと理由を残し、外部衝突の解消後だけ安全に復元を再試行する。 |
| DI-234 | 4K画像で手描きモザイクと除外を作成して保存し、Networkで開始・各レイヤー・確定の要求を確認する | 各dirtyレイヤーは`application/octet-stream`で順に転送し、確定JSONにPNGのdata URLやbase64を含めない。ステージ済みPNGも通常のdata URLと同じPNG・寸法・alpha/grayscale検証を通過して保存される。保存後は候補、手描き、確認状態、PJ履歴を再読込後も一致させる。 |
| DI-235 | 手描きレイヤーの開始またはレイヤー応答をNetworkで失わせ、取消要求も失敗させてから、同じ画像を再び保存する。レイヤー転送または確定中に画像一覧を切り替え、送信失敗も行う | 転送・確定中の同一画像の開始・取消は処理中として拒否し、画像一覧切替は書込み中の一時データを消さず放棄予定にする。切断後はwriter claimを解放して次の開始が未完転送を放棄・回収する。確定失敗では手描きの表示とdirtyレイヤーを残し、未保存状態を表示して次の操作で再試行できる。成功した再試行は未保存表示を消す。すでに削除済みの一時ディレクトリは回収済みとして扱い、元のstale_catalogなどの失敗理由を変えない。切替または取消した未確定転送は永続化せず、部分PNG・別画像の手描き・候補・履歴を残さない。CMDには開始・レイヤーbytes・確定、または失敗理由・一時転送の破棄・確定済み一時データの片付けを区別して記録する。 |
| DI-236 | ブラウザー画像追加を開始して、正常完了、途中取消、通信断後の新しい追加を順に行う | 追加は明示的な開始・取消状態を含む完了で管理する。取消または切断した非稼働セッションは次の開始で置換し、30秒待ちや別セッションの画像追加を要求しない。CMDには固定ルートの開始・bytes・成功/失敗件数・完了または放棄を記録する。 |
| DI-237 | ブラウザー保存を準備後に応答を失い、同じ画像で再度準備してから確定する。別ケースで取消、画像一覧切替、アプリ終了後の再起動を行う | CIでreceiptとcleanupの永続化を確認する。実機では未確定tokenと一時出力の置換・回収、応答喪失後の再送、時刻だけで失効しないこと、CMDの開始・完了または失敗理由・所要を確認する。 |
| DI-238 | 手描き保存のDB確定後にステージングPNGを別プロセスで一時的に開き、確定応答と次の手描き保存を確認する | DB確定は成功のまま返し、削除できないステージングだけをCMDへ保留として記録する。次の手描き操作で削除を再試行し、候補・手描き・履歴・確認状態を巻き戻さない。 |
| DI-239 | 単体・一括・元画像削除の各ブラウザー保存で、commit送信前の永続ack intentとcommit成功後の`/api/save/ack`を確認する。commitまたはack応答を失わせ、タブを閉じてから再起動・画面復帰・状態確認を行う。別タブで別画像を同時に保存する | CIでack失敗時のreceipt保持と再ackを確認する。実機ではcommit応答喪失をstatusで確定してackし、pendingとunknownを区別し、別タブのtokenや削除済み画像のreceiptを誤って残さないことを確認する。 |
| DI-240 | 既存画像5,000件のPJを開き、元画像に変更がない場合と一部のサイズ・更新日時を変えた場合の読込時間・CMDログを確認する | 変換状態はSQLiteの実際のbind上限ごとの一括取得で照合する。画像ごとの追加SELECTを発生させず、変更画像だけsource flipを初期化して既存の候補・手描き・履歴・確認状態を保持する。 |
| DI-241 | 旧一覧を保持したままPJ切替・画像削除・候補保存を行い、サーバーが`stale_catalog`を返す状態にする | 画面は内部エラーではなく「画像一覧が変わりました」の案内を表示し、現在の一覧へ同期後に再実行できる。 |
| DI-242 | `image_transforms`テーブルまたはその必須列を欠いた同一バージョンの作業DBを用意し、読み取り専用で起動する | SQLiteがテーブルを作成・変更する前に、作業DBの再作成が必要な状態として検出する。既存DBを更新せず、通常の作業画面を開かない。 |
| DI-243 | 無名作業を閉じる経路で、その作業の画像を破棄する | 対象catalogの全画像だけを削除し、呼び出し側から相対パス集合を渡して部分削除する経路を持たない。 |
| DI-244 | ブラウザー画像追加で正常な1画像を送信し、別ケースで受信後のカタログ切替拒否または一時保存失敗を起こしてから、同じ接続で一覧取得を続ける | 正常追加は一覧へ公開し、失敗時は一時保存を残さずimport sessionを終了する。続く要求を本文の残りとして解釈せず、501 `Unsupported method`を出さない。 |
| DI-245 | 画像0件の無名作業でブラウザー画像を1件追加し、追加後に再起動して同じ作業を開く | 追加は500にならず、作成した無名作業を再起動後も開ける。active projectless catalogは作業1件だけを指し、追加画像・一覧・候補を保持する。 |
| DI-246 | 名前付きPJを開いてモザイクマスクPNGを単体で書き出し、DeprecationWarningをエラーとして扱う環境でも応答を確認する | `/api/project/mask/<id>/mosaic` は500ではなくL形式のPNGを返す。警告を例外化しても書出し結果とPJ状態を変えない。 |
| DI-247 | 元画像削除の後処理が保留された削除トークンをackし、サーバーが`source_delete_cleanup_pending`を返す状態にする | 画面は内部エラーではなく、削除後処理の完了待ちを案内する。完了後の同じackは通常どおり削除操作を回収する。 |
| DI-248 | 候補を含む2万画像のPJと複数元フォルダーPJをそれぞれ開く | 各PJを開く間、候補BLOBの一括読込は1回だけ行う。候補、確認済み、非表示、反転、元画像変更状態を保持し、同じ候補BLOBを二重に読込まない。 |
| DI-249 | 元画像を変更せずにブラウザー保存を1件実行し、保存応答の受信中と完了後に一覧を確認する | 保存レンダーは画像レスポンスと保存トークンを返し、500や空応答を返さない。一時ファイルは応答完了後に回収し、画像・候補・確認状態を変えない。 |
| DI-250 | 自動検出中に、候補PNGを書き出した直後の元画像再照合で元画像を外部変更する | 検出は失敗として終了し、未公開候補の一時PNGを残さない。既存候補・手描き・確認状態・一覧を変更しない。 |
| DI-255 | 名前付きPJと無名作業で、確認済み画像の候補を単体変更・一括変更・削除する。各操作の直後に一覧APIを取得し、永続化失敗も発生させる | 成功時は候補と確認済み状態を同時に公開し、対象は未確認となる。失敗時は候補・確認済み状態をどちらも操作前のまま保持する。 |
| DI-256 | 候補マスクを画像ビューアーで開いたまま、候補の単体削除と役割ごとの一括削除を実行し、各操作の後にUndo、Redo、PJ再開を行う | Windowsが古い候補PNGの削除を一時的に拒否しても削除操作は成功し、候補revisionと履歴は各1回だけ進む。画面とPJ再開は候補を削除済みとして一致し、他候補を変えない。 |
| DI-257 | ブラウザーdirectory sourceの同じ相対名画像を外部変更して再読込する。読込後半を中断する場合と完了する場合を分け、反転済み画像のPJ再開状態も確認する | 成功時だけ外部変更としてsource flipを初期化して新しい寸法を公開する。中断・読込失敗では変換フラグ、revision、既存一覧を開始前へ戻し、途中画像・一時sourceを残さない。 |
| DI-251 | PJの元画像寸法変更により、保存済み手描きマスクを新しい寸法へ再配置する。DeprecationWarningをエラーとして扱う環境でも実行する | 二値マスクをL形式PNGとして再配置し、警告を例外化しても500にならない。候補・手描き・履歴・確認状態を寸法不一致のまま公開しない。 |

| DI-252 | rename後の元パスと隔離ファイルを外部置換し、共有・権限拒否・I/O到達不能後に再起動復旧する | 元パス・隔離名・fingerprint・128bit FileIdが一致しない場合は上書き・削除しない。FileIdが全0または取得不能なら所有証明に使わず保留する。 |
| DI-253 | ブラウザーまたは単体・一括の上書き保存で元画像のbackup作成後、workspace確定前にMozarieを強制終了して再起動する。別ケースでは元パスまたはbackupを外部で差し替える。 | receiptなしではMozarie自身が置いた置換画像だけを元画像へ戻し、receiptありでは同じ所有証明のbackupだけを回収する。外部差し替えがある場合は元画像・backupとも上書き・削除しない。 |
| DI-254 | 名前付きPJのマスクZIP書出し中にMozarieを強制終了して再起動し、通常のZIP書出しも行う。 | 一時ZIPは実行用cache配下にだけ作成され、再起動時のcache整理で残留分を回収する。通常の応答は完了後にZIPを削除し、ダウンロード内容とPJ状態を保持する。 |
| DI-259 | 1MiBを超えるzTXtまたはiTXtを持つPNGを、ブラウザーの「画像を選択」と「フォルダーを選択」から、通常PNGと混在させて読み込む。読込後に原画像とメタ情報を照合する | 一時転送名の拡張子に左右されず全画像を読み込み、一覧・サムネイル・編集画面に表示する。原画像のバイト列とメタ情報を削除・変換しない |
| DI-260 | 候補revisionが保存領域だけ先へ進んだ状態から全画像自動検出を開始し、続けて手描き編集、絶対パスへのコピー保存、確定、受領、PJ再開を行う | 検出開始前に保存領域へ再同期して500にせず、遅い競合は`catalog_changed`として案内する。保存画像へモザイクを反映し、大きいPNGメタ情報を保持し、再開後の候補・履歴・確認状態を一致させる |
| DI-261 | PJ Aで候補・3手描き層・非表示・確認済みを設定し、PJ Bを開いて別の候補・選択・フィルターを設定してからAへ戻る。Aで非表示のUndo、Redo、マスクZIP書出しも行う | Aだけの候補・手描き・履歴・非表示/確認済みを復元し、Bの選択・検索・フィルターを持ち込まない。Undoは画像を再表示して編集・保存を再有効化し、Redoは再び無効化する。ZIPは非表示画像を含めない。 |
