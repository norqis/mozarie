# 検出・モデル・設定・ショートカット

[共通確認](../manual-verification.md)を全行で実施する。検出精度は評価せず、スイッチ・対象・実行経路・保存状態を確認する。

| ID | 手順 | 期待値 |
| --- | --- | --- |
| SD-001 | 設定を開く | 設定タブと保存・初期値・閉じるを表示する |
| SD-002 | 一般タブを選ぶ | 一般の項目だけが表示され、タブの選択表示が移る |
| SD-003 | 検出タブを選ぶ | 検出の項目だけが表示され、タブの選択表示が移る |
| SD-004 | 検出タブ内のモデル一覧へスクロールする | 各モデルのスイッチ・説明・準備操作を表示する |
| SD-005 | 表示・操作タブを選ぶ | 表示・操作の項目だけが表示され、タブの選択表示が移る |
| SD-006 | ショートカットタブを選ぶ | ショートカットの項目だけが表示され、タブの選択表示が移る |
| SD-007 | 確認タブを選ぶ | 確認の項目だけが表示され、タブの選択表示が移る |
| SD-008 | 情報タブを選ぶ | 情報の項目だけが表示され、タブの選択表示が移る |
| SD-009 | 言語を日本語から英語へ変更 | 開いている画面と候補・ダイアログの表示が英語へ切り替わる |
| SD-010 | 言語を英語から日本語へ変更 | 画面が日本語へ切り替わり、翻訳キーを表示しない |
| SD-011 | 設定を変更して保存 | 保存完了後に設定を開き直すと保存値が表示される |
| SD-012 | 保存済み設定でサーバー再起動 | 各保存値を復元する |
| SD-013 | 設定を初期値へ戻す | 初期値が表示され、保存後の再表示にも反映される |
| SD-014 | 設定の閉じるを押す | 設定画面が消えて元の画像へ戻る |
| SD-015 | 設定でEscapeを押す | 設定画面が消え、背景の描画を実行しない |
| SD-016 | 一般の自動ブラウザー起動をOFFにして再起動 | 自動で新しいブラウザーを開かない。URLを手動で開くと利用できる |
| SD-017 | 一般の自動ブラウザー起動をONにして再起動 | 起動URLのブラウザーを開く |
| SD-018 | 読み込み並列数を変更して読み込む | 設定値を保持し、全画像の読み込み完了後に操作を再有効化する |
| SD-019 | 保存並列数を変更して保存 | 設定値を保持し、出力の重複・欠落が発生しない |
| SD-020 | 検出対象の陰茎だけON | 送信対象が陰茎だけとなり、未選択クラスの新規候補を作らない |
| SD-021 | 検出対象の膣だけON | 送信対象が膣だけとなり、未選択クラスの新規候補を作らない |
| SD-022 | 検出対象を両方ON | 両対象を送信して処理を開始できる |
| SD-023 | 検出対象を両方OFF | 対象の選択を求め、検出を開始しない |
| SD-024 | NTD11をOFFにして現在画像を検出 | NTD11の設定がOFFで送られ、そのモデル由来の新規候補・除外を生成しない。既存候補の存在を新規検出と混同しない |
| SD-025 | NTD11をONにして現在画像を検出 | NTD11がONで保存・送信され、利用可能なら準備・実行される。検出精度と候補数の最低値は要求しない |
| SD-026 | NTD11をOFFで保存し再起動 | NTD11のOFFが維持される。他モデルのスイッチを変えない |
| SD-027 | SensitiveをOFFにして現在画像を検出 | Sensitiveの設定がOFFで送られ、そのモデル由来の新規候補・除外を生成しない。既存候補の存在を新規検出と混同しない |
| SD-028 | SensitiveをONにして現在画像を検出 | SensitiveがONで保存・送信され、利用可能なら準備・実行される。検出精度と候補数の最低値は要求しない |
| SD-029 | SensitiveをOFFで保存し再起動 | SensitiveのOFFが維持される。他モデルのスイッチを変えない |
| SD-030 | 手検出をOFFにして現在画像を検出 | 手検出の設定がOFFで送られ、そのモデル由来の新規候補・除外を生成しない。既存候補の存在を新規検出と混同しない |
| SD-031 | 手検出をONにして現在画像を検出 | 手検出がONで保存・送信され、利用可能なら準備・実行される。検出精度と候補数の最低値は要求しない |
| SD-032 | 手検出をOFFで保存し再起動 | 手検出と、それに依存する手領域分割がOFFで維持される。NTD11・Sensitive・白い液の設定は保持する |
| SD-033 | 手領域分割をOFFにして現在画像を検出 | 手領域分割の設定がOFFで送られ、そのモデル由来の新規候補・除外を生成しない。既存候補の存在を新規検出と混同しない |
| SD-034 | 手検出と手領域分割をONにして現在画像を検出 | 両方のONが保存される。手候補がある場合に手領域分割を実行する。手候補0件なら実行確認済みとしない |
| SD-035 | 手領域分割をOFFで保存し再起動 | 手領域分割のOFFが維持される。他モデルのスイッチを変えない |
| SD-036 | 白い液をOFFにして現在画像を検出 | 白い液の設定がOFFで送られ、そのモデル由来の新規候補・除外を生成しない。既存候補の存在を新規検出と混同しない |
| SD-037 | 白い液をONにして現在画像を検出 | ONが保存・送信される。性器候補がある場合に白色領域処理を実行する。性器候補0件なら実行確認済みとしない |
| SD-038 | 白い液をOFFで保存し再起動 | 白い液のOFFが維持される。他モデルのスイッチを変えない |
| SD-039 | 輪郭補正をOFFで検出 | SAMの準備を要求せず、輪郭補正なしで完了する |
| SD-040 | 輪郭補正をONで検出 | 選択したSAMを利用し、未準備ならモデル準備の案内を出す |
| SD-041 | vit_bを選んで保存・再表示 | vit_bだけが選択され、再表示後も保持される。未準備ファイルを別の種類で代用しない |
| SD-042 | vit_lを選んで保存・再表示 | vit_lだけが選択され、再表示後も保持される。未準備ファイルを別の種類で代用しない |
| SD-043 | vit_hを選んで保存・再表示 | vit_hだけが選択され、再表示後も保持される。未準備ファイルを別の種類で代用しない |
| SD-044 | CPUを選択して検出 | CPUで処理し、GPU必須エラーを表示しない |
| SD-045 | CUDA GPUを選択して検出 | 選択した物理GPUで処理する |
| SD-046 | DirectML GPUを選択して検出 | 選択した物理GPUで処理する。環境にDirectMLがなければ未実施と報告する |
| SD-047 | 利用不能なGPUで検出 | GPU失敗として案内し、CPU専用セッションへ切り替えない |
| SD-048 | 検出の標準・最高精度を切り替える | 選択した実行モードを保存して検出へ渡す。精度の比較評価は実施しない |
| SD-049 | 信頼度スライダーを変更 | 数値表示と送信値が一致する |
| SD-050 | 全画像検出の信頼度数値を変更 | スライダーと数値が一致し、その値で開始する |
| SD-051 | 全画像検出の枠を変更 | 新規候補へ指定値を反映し、閉じて再表示しても保持する |
| SD-052 | 現在画像の自動検出を押す | 現在画像だけを対象に進捗画面を表示し、完了後に編集できる |
| SD-053 | 非表示画像を含む全画像自動検出を開く | 非表示を除く対象数と設定を表示し、開始前は画像を処理しない |
| SD-054 | 全画像検出の開始前に取消 | 設定画面が閉じ、検出を実行しない |
| SD-055 | 全画像検出を開始 | 非表示を除く対象数に対して進捗が進み、準備表示とファイル名を交互に点滅させない |
| SD-056 | 初回に80画像中20画像を非表示にして全画像検出 | 開始時から60件の対象・進捗を表示し、処理終了後に画面操作を再有効化する。検出精度は評価しない |
| SD-057 | 検出を一時停止 | 新規画像処理を止め、一時停止状態を表示する |
| SD-058 | 検出を再開 | 未処理対象から再開し、完了済み画像を二重追加しない |
| SD-059 | 検出をキャンセル | 処理を停止し、完了済みと未処理を混同せず、編集を再有効化する |
| SD-148 | 3件以上の全画像検出を開始し、各画像の検出終了と全件公開直前を観察する。途中で1件を失敗させる操作も別途行う。候補公開後、古い候補マスクの片付け中に取消を押す | 候補公開前でも進捗は1/NからN/Nへ進み、処理中の画像名と残り時間を表示する。公開までは完了ID・候補一覧を増やさず、全件推論済みの公開待ち状態では一時停止を無効表示にして、取消は公開開始まで使える。失敗時は処理済み件数を表示しても候補IDと候補内容を一切公開しない。公開後の取消は受け付けず、公開済み候補を残して完了する |
| SD-060 | 確認済みと未確認を混在させて検出 | 検出前の各画像の確認状態を保持する |
| SD-061 | 最高精度で複数sourceが同一対象に合意し、手との重なりが15%超70%以下の候補を検出 | 通常選択不能時だけ、手との重なりが少ないSAM候補を優先する。初回候補と手が重なる場合は、初回のpoint・box・low-res maskを保った再推定で、重なり内へ既存分と合計3点までのnegative点を空間分散して追加する。SAMは輪郭補正だけに使い、通常の検出器thresholdを通過したAPPLY候補はSAMの選択不成立でも保持する。手は独立したEXCLUDE候補として操作できる |
| SD-062 | 同じ検出対象を標準と最高精度でそれぞれ実行する | 各実行でauto候補を新しい結果へ置換する。境界候補・手描き範囲・別画像の候補を消さない |
| SD-070 | 最高精度で、SAM候補が選択ゲートを通らない状態を用意して実行する | 検出器の候補を`sam_fallback`として残す。空候補に置換せず、他候補・別画像・手描き範囲を変更しない |
| SD-071 | 最高精度でSAM候補が選択ゲートを通る対象を実行する | `sam_high_precision`の補正済み候補を作る。検出器マスクへ戻さず、候補の保存・再読込・ON/OFFを維持する |
| SD-143 | 手検出と最高精度をONにし、対象と重なる手を含む画像を実行して、APPLY候補と手のEXCLUDE候補を個別にON/OFFする | APPLYと手のEXCLUDEを別々の候補として表示する。手だけをON/OFFしてもAPPLY候補の候補行・ON/OFF・手描き範囲を変えず、保存後のモザイク範囲だけを更新する |
| SD-146 | 透明外側に目立つRGB値を持つ透過PNGを自動検出し、通常検出と最高精度・境界候補の各結果を確認する | 推論は透明外側を黒の背景として扱い、APPLY・手・白い液を含む全候補マスクはalphaが0の画素へ出ない。候補数や検出精度は評価しない |
| SD-147 | 同一クラスの離れた2候補と両方を覆う補助候補を含む最高精度検出を実行する | 補助候補は重なり・包含率・source優先度で最も近い1候補だけの合意として扱い、別候補へ合意状態を広げない。多数候補時も手領域の対象範囲を処理し、全マスク配列の積み上げにより処理を停止しない |
| SD-063 | 基本モデルの説明を開く | 基本モデルの用途・正式名称・ONNX形式・配布元を表示する |
| SD-097 | NTD11の説明を開く | NTD11の用途・配布元・変換コマンドを表示する |
| SD-098 | Sensitiveの説明を開く | Sensitiveの用途・配布元・変換コマンドを表示する |
| SD-099 | 輪郭補正の説明を開く | SAMの用途・配布元とvit_b・vit_l・vit_hの比較表を表示する |
| SD-100 | 手検出の説明を開く | 画像全体から手を検出する用途・ONNX形式・配布元を表示する |
| SD-101 | 手領域分割の説明を開く | 検出した手の輪郭を補正する用途・safetensors形式・配布元を表示する |
| SD-102 | 白い液の説明を開く | 性器候補内の白色領域を対象とし、追加モデルが不要であることを表示する |
| SD-064 | モデル説明を閉じる | 設定画面へ戻り、選択モデルの値を保持する |
| SD-065 | モデル準備コマンドのコピーを押す | 表示しているコマンドとコピー内容が一致する |
| SD-066 | 未準備モデルのダウンロードを開く | 対象モデル名と開始操作を表示する |
| SD-067 | モデルのダウンロードを開始 | 進捗を表示し、完了後に利用可能状態になる |
| SD-068 | モデルダウンロードを取消 | 中止を表示し、未完了ファイルを利用可能と表示しない |
| SD-069 | 未準備モデルのダウンロード開始前に確認用サーバーを停止し、開始を押す | サーバーとの通信失敗を表示し、未取得モデルを利用可能と表示しない |
| SD-072 | 濃さを変更 | 範囲表示の不透明度だけが変わり、保存画像のモザイク範囲は変わらない |
| SD-073 | 設定のモザイク表示をOFF→ON | 編集中画像のモザイク表示が切り替わり、保存対象範囲は保持する |
| SD-074 | 新しい除外候補の強制初期値をOFF→ON | 変更後に追加される除外候補に初期値を適用し、既存候補を一括変更しない |
| SD-075 | ショートカット全体をOFFに保存し、描画後にCtrl+Zを押す | 描画と履歴位置が変わらない。画面のUndoボタンは使用できる |
| SD-103 | ショートカット全体OFFでUndoボタンを押した後、Ctrl+Shift+Zを押す | キーではRedoせず、画面のRedoボタンでだけ描画を復元できる |
| SD-104 | ショートカット全体OFFで画像上にフォーカスし、左右矢印を押す | 表示画像と画像番号が変わらない。画面の前へ・次へボタンは使用できる |
| SD-105 | 初期設定を表示する | 削除ショートカットは Delete・ONである |
| SD-106 | 削除ショートカットを保存して設定を開き直す | 保存値を表示する |
| SD-107 | 削除ショートカットの保存後に再起動する | 保存値を復元する |
| SD-108 | 設定を初期値へ戻す | 削除ショートカットは Delete・ONに戻る |
| SD-109 | 削除ショートカットのない旧設定で、Delete が未使用のまま起動する | 削除ショートカットは Delete・ONで補完される |
| SD-110 | 別操作へ `Delete` または前後空白付きの ` Delete ` を割当済みの旧設定で起動する | 正規化後の既存 Delete を保ち、削除ショートカットは未使用の修飾キー付き Delete・OFFで補完され、設定を保存できる |
| SD-111 | 削除確認を取消する | 対象と元画像ファイルを保持する |
| SD-112 | 削除確認を承認し、フィルター中の画像を削除する | 元画像ファイルと一覧・DB上の対象を完全削除し、次の表示対象へ移る |
| SD-113 | 削除ショートカットを個別にOFFにする | Delete で削除・確認を開始しない |
| SD-114 | ショートカット全体をOFFにする | Delete で削除・確認を開始しない |
| SD-115 | 削除ショートカットを別キーへ再割当てする | 新しい割当だけが削除を開始する |
| SD-116 | 削除ショートカットと他操作へ同じキーを割当てて保存する | 重複を案内し、複数操作を同時実行しない |
| SD-117 | input・textarea・select・contenteditable、候補削除などの操作button、左一覧の現在画像以外のカード、一覧表示のカードで Delete を押す | 通常の編集・操作を妨げず、削除しない |
| SD-118 | 設定のキー割当欄で Delete を押す | 割当欄に Delete を記録し、画像を削除しない |
| SD-119 | 確認ダイアログ表示中に Delete を押す | ダイアログ外の削除を開始しない |
| SD-120 | 一覧表示で Delete を押す | 画像を削除しない |
| SD-121 | 処理中・読み込み中に Delete を押す | 削除を開始しない |
| SD-122 | 読取専用PJ、PJ切替中、画像寸法不一致、画像切替待機中に Delete を押す | 削除を開始しない |
| SD-123 | 削除ショートカットを押し続ける | 最初の有効な1回だけ削除確認を開き、繰返しで削除要求を増やさない |
| SD-124 | 削除時に409を返す | 削除せず、画面と選択状態を整合させる |
| SD-125 | 削除時に通信を切断する | 画面と選択状態を整合させる |
| SD-126 | 編集表示で左一覧の現在画像カードをクリックし、フォーカスが残った直後に Delete を押す | 既存の削除確認を開く。確認後は対象画像だけを削除する。他のbutton、現在画像以外のカード、一覧表示、ダイアログ中では削除を開始しない |
| SD-076 | ショートカット全体をON | 有効な登録キーで対応操作を実行する |
| SD-077 | 前へのショートカットを割り当て、個別ON→OFF | ONでは前へを実行し、OFFでは同じキーで実行しない |
| SD-078 | 次へのショートカットを割り当て、個別ON→OFF | ONでは次へを実行し、OFFでは同じキーで実行しない |
| SD-079 | 前の表示画像のショートカットを割り当て、個別ON→OFF | ONでは前の表示画像を実行し、OFFでは同じキーで実行しない |
| SD-080 | 次の表示画像のショートカットを割り当て、個別ON→OFF | ONでは次の表示画像を実行し、OFFでは同じキーで実行しない |
| SD-081 | 先頭のショートカットを割り当て、個別ON→OFF | ONでは先頭を実行し、OFFでは同じキーで実行しない |
| SD-082 | 末尾のショートカットを割り当て、個別ON→OFF | ONでは末尾を実行し、OFFでは同じキーで実行しない |
| SD-083 | 確認済みにして次へのショートカットを割り当て、個別ON→OFF | ONでは確認済みにして次へを実行し、OFFでは同じキーで実行しない |
| SD-084 | 一覧表示のショートカットを割り当て、個別ON→OFF | ONでは一覧表示を実行し、OFFでは同じキーで実行しない |
| SD-085 | 元に戻すのショートカットを割り当て、個別ON→OFF | ONでは元に戻すを実行し、OFFでは同じキーで実行しない |
| SD-086 | やり直すのショートカットを割り当て、個別ON→OFF | ONではやり直すを実行し、OFFでは同じキーで実行しない |
| SD-087 | 重複ショートカットを保存 | 重複を案内し、競合したまま複数操作を同時実行しない |
| SD-088 | 数値・名前入力欄で登録キーを押す | テキスト編集を行い、背後の画像移動・描画を実行しない |
| SD-089 | モザイククリアの確認設定をON→OFF | ONでは実行前に確認し、OFFでは確認を省略する。実行対象は同じ |
| SD-090 | 一覧クリアの確認設定をON→OFF | ONでは実行前に確認し、OFFでは確認を省略する。実行対象は同じ |
| SD-091 | 画像削除の確認設定をON→OFF | ONでは実行前に確認し、OFFでは確認を省略する。実行対象は同じ |
| SD-092 | 候補削除の確認設定をON→OFF | ONでは実行前に確認し、OFFでは確認を省略する。実行対象は同じ |
| SD-093 | 同種候補全削除の確認設定をON→OFF | ONでは実行前に確認し、OFFでは確認を省略する。実行対象は同じ |
| SD-094 | 上書きの確認設定をON→OFF | ONでは実行前に確認し、OFFでは確認を省略する。実行対象は同じ |
| SD-095 | コピー後元画像削除の確認設定をON→OFF | ONでは実行前に確認し、OFFでは確認を省略する。実行対象は同じ |
| SD-096 | 情報タブで更新確認 | 現在版と確認結果を表示し、更新がある場合だけ更新操作を案内する |
| SD-127 | 全画像自動検出を開き、モザイク候補の枠を3px、除外候補の枠を11pxにして開始する | 2つの入力を区別して表示し、新規のモザイク候補だけに3px、新規の手・白い液などの除外候補だけに11pxを設定する。候補がない種類は精度確認済みと扱わない |
| SD-128 | SD-127の設定後、現在画像だけを自動検出する | 保存済みの2値を使い、新規のモザイク候補だけに3px、新規の除外候補だけに11pxを設定する。対象外画像、既存の境界候補、手描き範囲は変更しない |
| SD-129 | 一覧表示で複数画像を選択して自動検出を開き、モザイク候補の枠を5px、除外候補の枠を13pxにして開始する | 選択対象だけを処理し、新規候補へ役割別の値を反映する。選択外画像と既存の境界候補・手描き範囲は変更しない |
| SD-130 | 全画像自動検出を開き、モザイク候補の枠を7px、除外候補の枠を17pxにして開始する | 非表示を除く全対象を処理し、各画像の新規候補へ役割別の値を反映する。画像ごとに候補の有無が異なっても別の役割の値を流用しない |
| SD-131 | モザイク候補と除外候補の枠を両方0pxにして、現在・選択・全画像の各自動検出を実行する | 各経路の新規候補を0pxで作成し、元の候補マスクを広げない。負数・小数・空欄では開始せず、0以上の整数を求める |
| SD-132 | 単一の候補枠だけを保存した旧設定を読み込む | 旧値をモザイク候補と除外候補の両方へ引き継ぎ、既存候補の値を変更せず、新形式で保存できる |
| SD-133 | モザイク候補と除外候補へ異なる枠を保存してMozarieを再起動し、全画像自動検出を開く | 再起動前の2値をそれぞれ復元し、入れ替えたり一方をもう一方で上書きしたりしない |
| SD-134 | 16384pxを超え、かつ対象画像の対角線以下となる枠を入力して自動検出を開始する。続けて対角線を超える値も入力する | 固定16384pxでは拒否しない。画像内で効果がある値はそのまま候補へ設定し、対角線を超える値は画像寸法から求めた最大有効値として扱い、巨大な処理用配列を確保しない |
| SD-135 | 選択・全画像の各自動検出を開く | 同じ設定モーダルに「精液候補を色で広げる」と0〜255の許容範囲を表示する。初期値はON・26で、手動塗りつぶしの許容範囲を変更しない |
| SD-136 | 色で広げるをONにし、許容範囲を0、26、255へ順に設定して検出を開始する | 0〜255の整数だけを受け付け、設定値を保存・再表示する。0では既存の精液候補だけを保持し、各検出で対象候補またはメタデータ検索範囲の外へ広がらない |
| SD-137 | 色で広げるをOFFにして検出し、ONへ戻して別の許容範囲で再検出する | OFFでは既存の白い液候補検出だけを使い、ONでは開始時の設定値だけを全対象へ一貫して使う。検出中の設定変更で途中画像だけの値を変えない |
| SD-138 | 透過PNGで精液候補の色拡張を実行する | 完全透明画素へ候補が広がらず、RGB画像および既存の白い液候補検出の条件は変わらない |
| SD-139 | 設定で白い液をOFFにしてから、選択・全画像の自動検出を開く | 精液候補を色で広げるスイッチと許容範囲を操作不可にし、OFF中は許容範囲欄の無効な文字列で開始を妨げない |
| SD-140 | 選択または全画像のモーダルで精液候補の色拡張を保存した後、「現在画像を自動検出」を押す | 現在画像は従来どおり直ちに検出を開始し、モーダルを開かない。保存済みの色拡張ON/OFFと許容範囲を開始時に使用する |
| SD-141 | 設定画面の各モデル欄、SAMの各種別、既定の保存先に相対パスを入力して保存する。続けて実在する絶対パスを保存して開き直す | CIで相対形式の拒否と旧設定移行を確認する。相対パスでは該当する設定タブと入力欄を示し「絶対パス」を案内して保存しない。成功時は正規化済みの絶対形式を表示し、モデル選択・案内・現在の設定を混ぜない。 |
| SD-142 | 旧local.jsonを持つ確認用コピーを実際に起動する | CIで一回限りの絶対化と保存を確認する。実機では起動後の設定表示、利用者の作業フォルダー、モデル読込案内を確認する。 |
| SD-144 | 一般タブで言語・ポート・並列数・ショートカットを未保存のまま保存先を選択し、選択後に保存する | 保存先だけを更新し、未保存の各入力値を保持する。OSの選択取消・失敗でも各入力値を保持し、保存時の送信内容へ含める。 |
| SD-145 | 確認用コピーの`.venv`へCUDAまたはDirectML選択と異なるONNX Runtimeを入れ、`.mozarie-ready`とruntime markerを残して`run.bat`を実行する | サーバーを開始せず、ONNX Runtimeの状態が一致しないことと`setup.bat`で修復する案内をCMDへ表示する。実GPU・モデルは読み込まない。 |
| SD-149 | GPUを選択した設定を残したまま、一般タブの保存先を実在する絶対パスへ変更する。相対パスと存在しないパスも入力し、選択を取消してから再度選択する | 保存先だけの変更ではGPUの診断やモデル読込を始めず、実在する絶対パスだけを保存する。取消・失敗後も選択ボタンを再度使える。OSの初期フォルダーは既存の絶対パスだけを使う。 |
