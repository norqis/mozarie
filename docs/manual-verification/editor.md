# 描画・境界・候補・表示・履歴

[共通確認](../manual-verification.md)を全行で実施する。塗りつぶし用に連結した近似色と離れた同色領域を用意する。

| ID | 手順 | 期待値 |
| --- | --- | --- |
| ED-001 | モザイク筆で左ドラッグ | 通過した円形ブラシ範囲に描画中からモザイクを追加する。除外を追加しない |
| ED-002 | モザイク筆で1回クリック | クリック位置を中心とするブラシ径の範囲を追加する |
| ED-003 | モザイク消しゴムで既存モザイクを左ドラッグ | 通過範囲のモザイクを消し、離れた範囲と除外を保持する |
| ED-004 | モザイク塗りつぶしで単色領域をクリック | クリック地点から連結する許容色領域だけを追加する。離れた同色領域には追加しない |
| ED-005 | 除外ブラシでモザイクに重ねて左ドラッグ | 通過範囲を除外し、モザイクを外す。モザイク候補へ手書き行を追加しない |
| ED-006 | 除外ブラシで1回クリック | クリック位置を中心とする円形除外を追加する |
| ED-007 | 除外消しゴムで既存除外を左ドラッグ | 通過範囲の除外を消す。下にモザイクがあれば再表示する |
| ED-008 | 除外塗りつぶしで単色領域をクリック | 連結する許容色領域へ除外だけを追加する |
| ED-009 | モザイク塗りつぶしの許容値を小→大へ変え、Undoを挟み同じ点をクリック | 小さい値では色差の大きい隣接領域を含めず、大きい値では許容された隣接色まで広がる |
| ED-010 | 除外塗りつぶしの許容値を小→大へ変え、Undoを挟み同じ点をクリック | 許容値に応じ除外だけが広がり、離れた同色領域は変わらない |
| ED-011 | 色許容小窓の閉じるを押す | 小窓が消え、塗りつぶしの選択は維持する |
| ED-012 | 色許容小窓でEscapeを押す | 小窓が消え、背景に残らない |
| ED-013 | 色許容小窓を開いて別ツールを選ぶ | 小窓が消え、選んだツールだけが選択表示になる |
| ED-014 | 色許容値を変更し、再読み込み・サーバー再起動後に開く | 保存値が表示され、次の塗りつぶしに使われる |
| ED-015 | ブラシ径を最小・中間・最大へ変更する | px表示と円の直径が連動する。変更だけで描画しない |
| ED-016 | ブラシ選択中にShift+ホイールを上下する | ブラシ径が増減し、画像倍率と一覧スクロール位置は変わらない |
| ED-017 | モザイク粗さの分母を変更する | 計算pxと粒度が変わり、マスク形状を保持する |
| ED-018 | 粗さの説明を開いて閉じる | 説明とリンクが表示され、閉じると編集画面へ戻る |
| ED-019 | 境界ボタンを押す | モザイクツール位置に矩形・4点・境界ブラシの選択肢が開く |
| ED-020 | 矩形境界で左ドラッグ | 始点と終点を対角とする矩形を表示する。検出前にモザイクを追加しない |
| ED-021 | 4点境界で画像内を4回クリック | クリック順に頂点を結ぶ閉じた領域を表示する。検出前にモザイクを追加しない |
| ED-022 | 境界ブラシで左ドラッグ | 通過範囲を境界領域として表示する。モザイクを追加しない |
| ED-023 | 作成した境界で取消を押す | 作成中の境界が消え、既存モザイクと除外は残る |
| ED-024 | 作成した境界で検出を押し、完了直後に追加候補の枠を開く | 指定範囲の候補を追加し、枠を入力できる。範囲外の既存編集を保持する |
| ED-025 | 画像外をクリック | 画像外にマスク・候補を追加しない |
| ED-026 | 描画中に画像端を越えて戻る | 画像内にだけ描画し、反対側の端へ線が飛ばない |
| ED-027 | 描画中に別画像を選択する | ドラッグ中は画像を切り替えず、別画像へ描画しない |
| ED-028 | 描画中にCtrl+Zを押す | 描画途中に履歴を復元しない。終了後にUndoできる |
| ED-029 | 描画中にウィンドウ外でボタンを離して戻る | 押していないマウス移動で描画し続けない |
| ED-030 | 画像上でホイールを上下する | 画像とマスクが同じ倍率・位置で拡大縮小する |
| ED-031 | 画像を中ボタンドラッグする | 画像とマスクが一緒に移動し、候補数・マスク形状は変わらない |
| ED-032 | 画像上で右クリック | 標準メニューを表示せず、クリックだけで描画しない |
| ED-033 | 全体表示を押す | 画像全体が編集領域に収まり、縦横比を保持する |
| ED-034 | 1枚表示を選ぶ | 1枚表示になり、1だけが選択表示になる |
| ED-035 | 2枚表示を選ぶ | 左に画像、右に画像寸法の黒背景と範囲を表示し、2だけが選択表示になる |
| ED-036 | 2枚表示の左でズーム | 右も同じ倍率・位置へ追従する |
| ED-037 | 2枚表示の右でズーム | 左も同じ倍率・位置へ追従する |
| ED-038 | 2枚表示の左をパン | 右の対応画素も同じ変位で移動する |
| ED-039 | 2枚表示の右をパン | 左の対応画素も同じ変位で移動する |
| ED-040 | 2枚表示の左でモザイク描画 | 左右の同じ画像座標にモザイクと範囲色を反映する |
| ED-041 | 2枚表示の右でモザイク描画 | 左右の同じ画像座標にモザイクと範囲色を反映する |
| ED-042 | 2枚表示の左で除外描画 | 左の該当モザイクが外れ、右の対応座標に除外色を表示する |
| ED-043 | 2枚表示の右で除外描画 | 左の該当モザイクが外れ、右の対応座標に除外色を表示する |
| ED-044 | 2枚表示でモザイク表示ON・点滅OFF | 左はモザイク画像、右は固定色。両方点滅しない |
| ED-045 | 2枚表示でモザイク表示ON・点滅ON | 左のモザイクは点滅せず、右の対象候補だけ点滅する |
| ED-046 | 2枚表示でモザイク表示OFF・点滅ON | 左は元画像、右の対象候補だけ点滅する |
| ED-047 | 左一覧の分割線を単クリック | 幅・比率がクリック前後で変わらない |
| ED-048 | 左一覧の分割線の左端・中央・右端からそれぞれ100px右へドラッグ | どこをつかんでも開始時の幅から100pxだけ広がる。押す・離すだけでは幅が変わらない |
| ED-049 | 左一覧の分割線をダブルクリック | 1920px幅用の既定幅へ戻る |
| ED-050 | 左一覧の分割線にTabで移動し左右矢印を押す | キーボード操作で幅が増減し、画像内容を変更しない |
| ED-051 | 右候補の分割線を単クリック | 幅・比率がクリック前後で変わらない |
| ED-052 | 右候補の分割線の左端・中央・右端からそれぞれ100px左へドラッグ | どこをつかんでも開始時の幅から100pxだけ広がる。押す・離すだけでは幅が変わらない |
| ED-053 | 右候補の分割線をダブルクリック | 1920px幅用の既定幅へ戻る |
| ED-054 | 右候補の分割線にTabで移動し左右矢印を押す | キーボード操作で幅が増減し、画像内容を変更しない |
| ED-055 | 中央の分割線を単クリック | 幅・比率がクリック前後で変わらない |
| ED-056 | 中央の分割線の左端・中央・右端からそれぞれ100px右へドラッグ | 境界が開始位置から100px右へ移る。ハンドル内のつかみ位置で移動量が変わらない |
| ED-057 | 中央の分割線をダブルクリック | 左右50%へ戻る |
| ED-058 | 中央の分割線にTabで移動し左右矢印を押す | キーボード操作で幅が増減し、画像内容を変更しない |
| ED-059 | 左一覧を閉じて再表示 | 画像と選択状態を保持する。閉じるは全画像操作の右にある |
| ED-060 | 右候補を閉じて再表示 | 候補と設定値を保持する |
| ED-061 | 候補1件をOFF | その候補だけ合成から外れ、他候補の状態は変わらない |
| ED-062 | 候補1件をON | その候補が合成へ戻り、元範囲を保持する |
| ED-063 | 候補1件の検出範囲表示をON→OFF | その候補の検出範囲表示だけ切り替わり、保存用モザイクは変わらない |
| ED-064 | 候補1件の適用範囲表示をON→OFF | その候補の適用範囲表示だけ切り替わり、有効状態は変わらない |
| ED-065 | 除外候補の強制をOFF→ON | 強制ONでは重なるモザイクから除外され、PJ再開後も設定を保持する |
| ED-066 | 候補の枠を0→10→0pxにする | 10pxで元範囲の周囲を拡張し、0pxで元範囲へ戻る。他候補の値は保持する |
| ED-067 | 候補の枠に0.1を入力して確定 | 整数の指定を案内し、候補の枠と範囲を変更しない |
| ED-068 | 枠入力中に上下矢印を押す | 数値だけが増減し、一覧とページがスクロールしない |
| ED-069 | 枠小窓を開いて閉じる | 小窓が消え、候補名と操作を覆わない |
| ED-070 | モザイク候補の一括ON/OFFを切り替える | モザイク候補全件が指定状態となり、別種候補の状態は変わらない |
| ED-071 | モザイク候補の検出範囲一括表示を切り替える | モザイク候補の検出範囲表示だけが全件切り替わる |
| ED-072 | モザイク候補の適用範囲一括表示を切り替える | モザイク候補の適用範囲表示だけが全件切り替わる |
| ED-073 | モザイク候補の枠を一括設定 | モザイク候補全件に指定値を反映し、別種の枠は保持する |
| ED-074 | モザイク候補の全削除を確定 | モザイク候補だけが消え、別種は残る |
| ED-075 | 除外候補の一括ON/OFFを切り替える | 除外候補全件が指定状態となり、別種候補の状態は変わらない |
| ED-076 | 除外候補の検出範囲一括表示を切り替える | 除外候補の検出範囲表示だけが全件切り替わる |
| ED-077 | 除外候補の適用範囲一括表示を切り替える | 除外候補の適用範囲表示だけが全件切り替わる |
| ED-078 | 除外候補の枠を一括設定 | 除外候補全件に指定値を反映し、別種の枠は保持する |
| ED-079 | 除外候補の全削除を確定 | 除外候補だけが消え、別種は残る |
| ED-080 | 候補1件の削除を取消 | 候補数・範囲・履歴位置が変わらない |
| ED-081 | 候補1件の削除を確定 | 指定候補だけが消え、合成結果を再表示する |
| ED-082 | モザイクと除外のある画像でモザイククリアを確定 | 両範囲・候補が消え、2枚表示の右に残像がない |
| ED-083 | 除外だけの画像でモザイククリアを確定 | 操作可能で、除外候補と右の範囲色が消える |
| ED-084 | 点滅中に別画像へ移動 | 移動先へ旧画像の点滅を持ち越さない |
| ED-085 | 操作後にUndoボタン | 現在画像の直前1操作を戻す |
| ED-086 | Undo後にRedoボタン | 戻した1操作を再適用する |
| ED-087 | Aを編集、Bを編集してAでUndo | Aの直前操作だけを戻し、Bの編集は保持する |
| ED-088 | Undo後に新しい描画 | 新操作が保存され、分岐前のRedoはできない |
| ED-089 | PJを閉じて再開しUndo | 現在画像の保存済み履歴を戻せる |
| ED-090 | 自動検出後にUndo | 検出で加わった候補とモザイクを検出前へ戻す |
| ED-091 | 一括候補操作後にUndo | 一括変更1回を操作前へ戻す |
| ED-092 | 履歴復元中に連打・画像切替 | 復元を重複実行せず別画像に反映しない。終了後に操作を再有効化する |
| ED-093 | 4096×4096画像に離れた境界候補を1件ずつ、計3件追加 | 追加ごとに候補数が1・2・3件となり、前に追加した範囲も表示し続ける |
| ED-094 | 3候補の4K画像から別画像へ移動し、元の画像へ戻る | 3候補とそれぞれの範囲・ON/OFF・枠の値が復元される |
| ED-095 | 3候補の4K画像で枠変更→Undo→Redo | 変更候補だけの枠を戻し、再適用する。残る2候補を表示し続ける |
| ED-096 | 3候補の4K画像と小さい画像を5回往復 | 4K画像の全候補を復元でき、往復ごとに以前の範囲画像を保持し続けない |
| ED-097 | 境界検出後に各候補のパーセント表示を見る | 値が0～100%の範囲内に収まる |
| ED-098 | モザイク候補1件の枠を0pxから正の値へ確定する | API成功直後に候補の適用範囲、合成マスク、モザイクプレビューを新しい枠で再描画する。画像切替・再読み込み・モザイク表示の切替を要求しない |
| ED-099 | 除外候補1件の枠を正の値から0pxへ確定する | API成功直後に除外の適用範囲と合成マスクを0pxで再計算し、除外されなくなったモザイク範囲と画像一覧のモザイク有無をその場で更新する |
| ED-100 | 複数のモザイク候補の枠を一括で0pxから正の値へ確定する | API成功直後に対象のモザイク候補全件だけを新しい枠で再取得し、合成マスク・プレビュー・画像一覧のモザイク有無を更新する。除外候補の枠は変更しない |
| ED-101 | 複数の除外候補の枠を一括で正の値から0pxへ確定する | API成功直後に対象の除外候補全件だけを0pxで再取得し、合成マスク・プレビュー・画像一覧のモザイク有無を更新する。モザイク候補の枠は変更しない |
| ED-102 | 個別と一括の候補枠変更でAPIを409と通信切断にする | サーバーの候補リビジョンを再取得して、候補値・適用範囲・合成マスク・プレビュー・画像一覧のモザイク有無を確定前へ戻す。成功表示や未確認化を残さず、別画像へ変更を混ぜない |
| ED-103 | 名前付きPJでモザイク候補と除外候補の枠を個別変更し、それぞれ直後にUndo→Redoする | Undoを変更直後から有効にし、候補値・適用範囲・合成マスク・プレビュー・画像一覧のモザイク有無を変更前へ戻す。Redoで同じ範囲を再適用する |
| ED-104 | 名前付きPJでモザイク候補と除外候補の枠を役割ごとに一括変更し、それぞれ直後にUndo→Redoする | 一括変更を役割ごとの1操作として直後からUndoでき、対象候補全件の値と画面表示を原子的に戻す。Redoも全件へ再適用し、別の役割と別画像を変更しない |
| ED-105 | モザイク、モザイク消しゴム、除外、除外消しゴム、境界ブラシを各1回クリックする | ドラッグなしでもカーソル円と同じ半径だけを描画または消去し、各クリックは1回のUndoになる |
| ED-106 | 4K画像で各ブラシを高速にドラッグし、多数候補がある状態で描画する | ポインター位置に追従し、候補数に比例して全画面を複製しない。CPU・RAM使用量が描画回数ごとに増え続けない |
| ED-107 | モザイク追加を直線でドラッグし、円カーソルの外側に置いたcontrol pixelを確認してUndo→Redoする | 円の掃引外を変更せず、Undo→Redo後も同じ形になる |
| ED-108 | モザイク追加を90度に折ってドラッグし、円カーソルの外側に置いたcontrol pixelを確認してUndo→Redoする | 角の外側へmiter突起を作らず、Undo→Redo後も同じ形になる |
| ED-109 | モザイク追加を鋭角に折返して高速ドラッグし、円カーソルの外側に置いたcontrol pixelを確認してUndo→Redoする | 高速入力でも円の掃引外を変更せず、鋭角のmiter突起がなく、Undo→Redo後も同じ形になる |
| ED-110 | モザイク消去、除外追加、除外消去をそれぞれ直線、90度、鋭角折返しでドラッグし、カーソル外control pixelを確認してUndo→Redoする | 追加側と消去側のどちらも円の掃引外を変更せず、Undo→Redo後も同じ形になる |
| ED-111 | 検出範囲または適用範囲を役割ごとにONにし、手描き、境界候補、自動検出を追加してUndo→Redoする。手描き開始直後に取消も行う | 選択した役割の新規範囲も同じ表示方式で点滅する。取消で画素が残らない場合は手描き範囲の表示IDも残さない。個別表示操作、削除、画像またはカタログ切替では役割の継承を解除する |
| ED-112 | 色許容小窓の−と＋を連打し、0と255でも押す。通信応答を遅延させる | 1回ごとに1だけ変化し、端では対応側が無効になる。最後の操作値だけを保存し、応答順が逆でも表示と保存値を巻き戻さない |
| ED-113 | 自動候補、手描きモザイク、手描き除外、除外消しゴムの各候補行をTabで操作する | 各行でON/OFFが左端、削除×が右端にあり、Tab順・読み上げ順もON/OFFから削除×までの視覚順と一致する。対象を取り違えない |
| ED-114 | 無名作業で手描き、塗りつぶし、候補ON/OFF・強制・枠・削除、役割別一括操作、境界候補、反転を各1回実行し、ボタンとCtrl+Z/Ctrl+Shift+Zで戻す | 各操作が1履歴で、ボタンとショートカットの双方で戻し直せる。別画像へ混ざらず、再読み込み後も履歴基底と候補状態を復元する |
| ED-115 | 名前付きPJで手描き、塗りつぶし、候補ON/OFF・強制・枠・削除、役割別一括操作、境界候補、反転を各1回実行し、ボタンとCtrl+Z/Ctrl+Shift+Zで戻す | 各操作が永続履歴で、PJ再開後も戻し直せる。別画像・別PJの候補とマスクを変更しない |
| ED-116 | 無名作業で確認済み→未確認、非表示→再表示を各1回実行し、現在画像のボタンとCtrl+Z/Ctrl+Shift+Zで戻す | 確認済みと非表示を履歴へ1回ずつ記録し、非表示中もUndoで再表示できる。確認済み集合、フィルター、一覧表示を一致させる |
| ED-117 | 名前付きPJで確認済み→未確認、非表示→再表示を各1回実行し、ボタンとCtrl+Z/Ctrl+Shift+Zで戻す | 各変更を永続履歴へ1回ずつ記録し、PJ再開後も非表示・確認済み・一覧状態を復元する |
| ED-118 | 名前付きPJで複数画像を選び、確認済み、未確認、非表示、再表示を各1回実行していずれかの対象でUndo→Redoする | 選択画像群を1グループ履歴として原子的に戻し直し、非対象画像・別PJを変更しない |
| ED-119 | 候補個別ON/OFF・強制・枠と役割別一括操作を、通信切断と409で失敗させてからUndo/Redo状態を確認する | 失敗した操作は履歴を増やさず、候補値、確認済み、範囲表示、プレビュー、一覧を成功前へ戻す |
| ED-133 | 名前付きPJで履歴がない画像を開き、Ctrl+Z、設定済みのRedoキー（Ctrl+YまたはCtrl+Shift+Z）を各3回押す | 通信・暗転・ローディング・候補再読込・画像再選択を起こさず、編集／一覧表示と現在画像・選択を保持する。ブラウザー標準の履歴操作にも渡さない |
| ED-134 | 名前付きPJの1画像で離れた位置にモザイク、除外、除外消しゴムを描き、Ctrl+Z→Redoを各1回行う。編集表示と一覧表示の両方で、別画像も選択してから確認する | 対象の局所画素だけを戻し直し、他の手描き層・離れた画素・別画像を変えない。直後のUndoは保存待ちでも有効で、復元中も表示モード、現在画像、一覧の選択を保持する |
| ED-128 | 手描きモザイク・手描き除外・手描き除外消去と、検出済みモザイク・除外候補を用意し、読み込み中、保存中、処理中に各候補行を見る | 手描き行はON/OFF、手描き除外の強制、各削除を無効化する。検出済み行はON/OFF、強制、枠、削除を無効化する。検出範囲・適用範囲の表示操作は維持し、完了後に候補の状態を変えず操作を再開できる |
| ED-126 | モザイク候補・除外候補の個別枠と役割別一括枠を変更し、APPLY/EXCLUDE/強制除外が重なる箇所を、モザイク表示ON・OFFと拡大縮小の両方で確認する | API成功直後に新しい候補ビットマップで合成マスク、一覧のモザイク有無、範囲表示、モザイクプレビューを再計算する。未保存の手描きや履歴を追加せず、各役割の優先関係を維持する |
| ED-127 | 個別・一括の枠確定直後にキャンバスをクリック・ドラッグし、通信失敗時も同じ操作を行う | 枠更新中と失敗後の復元中は筆、塗りつぶし、除外、境界の描画を開始しない。小窓外の確定クリックはキャンバスへ伝播せず、成功・失敗後は既存の画像切替、Undo、Redoを維持する |
| ED-129 | 画像を全体表示より小さくして、余白からモザイク、除外、塗りつぶし、境界矩形、4点境界、境界ブラシを各1回開始する。続けて画像内から外へドラッグする | 余白からの開始は筆跡・塗りつぶし・境界点・Undo履歴を作らない。画像内から開始した操作は、画像端までを編集し、画像外へはみ出す座標を保存しない |
| ED-130 | 無名作業で手描き、候補ON/OFF・強制・枠、確認済み・非表示、反転を含む履歴を作り、Undo/Redo中に各APIを順に通信切断または409にする | 復元中は操作と履歴ボタンを重複実行しない。全保存が成功した場合だけ履歴位置を確定し、失敗時はサーバーの権威状態を再取得して候補、手描き、確認済み、非表示、反転、履歴位置と一覧を一致させる |
| ED-131 | 自動候補と手描きモザイク・手描き除外・除外消しゴムを同時に用意し、右ペインのモザイク側と除外側の「全削除」を順に実行してUndo→Redoする | 対象役割の自動候補と手描き範囲を同時に消し、別役割を保持する。1回の操作として戻し直せ、全削除後のボタンは無効になる |
| ED-132 | 画面にある全ボタン、チェックボックス、入力、選択肢を、候補・フィルター・モデル・PJ・ショートカットの各値ごとに操作する | OSダイアログを開く操作を除き、各操作が対応する画面状態、API、キャンバス、履歴のいずれかを変える。広いセレクターの先頭要素だけで他の値を代用しない |
| ED-120 | 読込後にメモリ不足になる大きな画像で矩形・4点境界を実行する | `internal_error`ではなく画像またはマスクの読み込み失敗を画面に示し、候補・履歴を追加せず、通常画像で境界検出を続けられる |
