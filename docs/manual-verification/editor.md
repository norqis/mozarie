# 描画・境界・候補・表示・履歴

決定的に再現できる確認は `tests/verification-contracts.editor.json` に移し、自動テストで実行する。ここにはCIで再現できない実環境の観測だけを残す。

| 観測キー | 観測 | 実環境 | CIで代替できない理由 |
| --- | --- | --- | --- |
| ED-106.2 | 4K画像で各ブラシを高速にドラッグし、多数候補がある状態で描画する → CPU・RAM使用量が描画回数ごとに増え続けない | Windows実機、4K画像、多数候補、実ポインティングデバイス、タスクマネージャーまたはプロファイラー | 実入力デバイスのイベント頻度とブラウザープロセスのCPU・常駐メモリ推移は、隔離CIの合成ポインター入力では再現できないため |
| ED-120.1 | 読込後にメモリ不足になる大きな画像で矩形・4点境界を実行する → `internal_error`ではなく画像またはマスクの読み込み失敗を画面に示し、候補・履歴を追加せず、通常画像で境界検出を続けられる | Windows実機で、通常画像を読み込んだ後に対象画像またはマスクの確保だけが失敗する実メモリ逼迫状態 | OSと画像ライブラリの実際のメモリ割当失敗を、CIプロセスや同居ジョブを不安定化させず決定的に発生させられないため |
